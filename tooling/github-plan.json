{
  "$comment": "Manifest of the GitHub working surface for Steel-Mission delivery. Applied by tooling/gh-plan-sync.py, which searches by title before it creates anything. Edit here, re-run the sync; do not hand-edit GitHub and expect it to survive.",
  "repository": "Present-Capable-Computing/Steel-Mission",
  "project": {
    "recordId": "PRJ-0001",
    "recordFile": "plan/PRJ-0001.json",
    "boardTitle": "Steel-Mission — Delivery",
    "boardDescription": "Steel-Mission delivery. PRJ-0000 usable surface and honest admin writes (complete); PRJ-0001 durable broker and remote pull-runner (active). Source of truth for the shape of this work is plan/ in the repository; this board is the working surface.",
    "boardVisibility": "public",
    "$commentVisibility": "The repository is public and every item on the board is a public issue, so a private board would hide a view of public information without protecting anything.",
    "fields": [
      {
        "name": "Phase",
        "dataType": "SINGLE_SELECT",
        "from": "label",
        "options": [
          "P0",
          "C0",
          "C1",
          "C2",
          "P1",
          "P2",
          "P3",
          "P4",
          "P5"
        ],
        "$comment": "Taken from the P0-P5 label already on every item, so the phase is not stated twice."
      },
      {
        "name": "Area",
        "dataType": "SINGLE_SELECT",
        "from": "label:area:",
        "options": [
          "store",
          "runner",
          "connector",
          "orchestration",
          "schema",
          "ci",
          "naming",
          "docs",
          "ui",
          "identity",
          "config-integrity"
        ],
        "$comment": "Taken from the area:* label. An item with no area label is left unset rather than guessed."
      },
      {
        "name": "Kind",
        "dataType": "SINGLE_SELECT",
        "from": "kind",
        "options": [
          "epic",
          "task",
          "acceptance criterion"
        ],
        "$comment": "An acceptance criterion is a task, but it is the criterion the milestone exists to satisfy, so it is worth seeing separately."
      },
      {
        "name": "Project",
        "dataType": "SINGLE_SELECT",
        "from": "label:prj:",
        "options": [
          "PRJ-0000",
          "PRJ-0001"
        ],
        "$comment": "Which project an item belongs to, taken from its prj: label. Two projects share one board."
      },
      {
        "name": "Stage",
        "dataType": "SINGLE_SELECT",
        "from": "label:stage:",
        "options": [
          "U0",
          "U1",
          "U2",
          "U3",
          "U4",
          "U5"
        ],
        "$comment": "PRJ-0000 stage. PRJ-0001 uses Phase; an item carrying neither label is reported unset rather than guessed."
      }
    ],
    "views": [
      {
        "name": "PRJ-0001",
        "layout": "TABLE_LAYOUT",
        "filter": "label:prj:PRJ-0001 -label:epic",
        "visibleFields": [
          "Title",
          "Phase",
          "Area",
          "Kind",
          "Status",
          "Milestone",
          "Assignees"
        ],
        "$comment": "PRJ-0001's tasks in phase order; this is the active project after PRJ-0000 closed MS-0012."
      },
      {
        "name": "By phase",
        "layout": "TABLE_LAYOUT",
        "filter": "-label:epic",
        "visibleFields": [
          "Title",
          "Phase",
          "Area",
          "Kind",
          "Status",
          "Milestone",
          "Assignees"
        ],
        "$comment": "The 52 tasks in phase order, epics filtered out so the list is work someone can pick up. Grouping by Phase is a UI click: the API exposes a view's filter and its visible fields, not its grouping."
      },
      {
        "name": "Epics",
        "layout": "TABLE_LAYOUT",
        "filter": "label:epic",
        "visibleFields": [
          "Title",
          "Phase",
          "Status",
          "Sub-issues progress",
          "Milestone"
        ],
        "$comment": "Six rows with their sub-issue progress. The rollup is native because the tasks are real sub-issues."
      },
      {
        "name": "Acceptance criteria",
        "layout": "TABLE_LAYOUT",
        "filter": "label:acceptance-criterion",
        "visibleFields": [
          "Title",
          "Phase",
          "Status",
          "Milestone"
        ],
        "$comment": "The tests the project exists to pass, in one place. If these are not green, the project is not done regardless of what else is."
      },
      {
        "name": "Board",
        "layout": "BOARD_LAYOUT",
        "filter": "-label:epic",
        "visibleFields": [
          "Title",
          "Phase",
          "Area",
          "Assignees"
        ],
        "$comment": "Status columns, for whoever is working today."
      }
    ]
  },
  "labels": [
    {
      "name": "epic",
      "color": "5319e7",
      "description": "A milestone-sized outcome that decomposes into tasks"
    },
    {
      "name": "task",
      "color": "0e8a16",
      "description": "A unit of delivery work someone can pick up and finish"
    },
    {
      "name": "acceptance-criterion",
      "color": "b60205",
      "description": "An automated test that settles a project-level criterion"
    },
    {
      "name": "security-review",
      "color": "d93f0b",
      "description": "Touches a trust boundary; needs review before merge"
    },
    {
      "name": "blocked",
      "color": "6a737d",
      "description": "Cannot proceed. Say why in the issue the day it happens"
    },
    {
      "name": "P0",
      "color": "fbca04",
      "description": "MS-0001 Toolchain and CI baseline"
    },
    {
      "name": "P1",
      "color": "1d76db",
      "description": "MS-0002 Durable broker service"
    },
    {
      "name": "P2",
      "color": "1d76db",
      "description": "MS-0003 Pull-runner agent and job protocol v2"
    },
    {
      "name": "P3",
      "color": "1d76db",
      "description": "MS-0004 Durable connector inbox and outbox"
    },
    {
      "name": "P4",
      "color": "1d76db",
      "description": "MS-0005 Single orchestration path"
    },
    {
      "name": "P5",
      "color": "1d76db",
      "description": "MS-0006 Steel-Mission naming"
    },
    {
      "name": "area:store",
      "color": "c5def5",
      "description": "Transactional store, schema migrations, state machine"
    },
    {
      "name": "area:runner",
      "color": "c5def5",
      "description": "Pull-runner agent, gRPC gateway, sandbox"
    },
    {
      "name": "area:connector",
      "color": "c5def5",
      "description": "Inbox, outbox, connector delivery"
    },
    {
      "name": "area:orchestration",
      "color": "c5def5",
      "description": "Mission lifecycle, controller, approvals"
    },
    {
      "name": "area:schema",
      "color": "c5def5",
      "description": "Authority-owned schemas and the registry"
    },
    {
      "name": "area:ci",
      "color": "c5def5",
      "description": "Build, test harness, continuous integration"
    },
    {
      "name": "area:naming",
      "color": "c5def5",
      "description": "Product naming migration"
    },
    {
      "name": "area:docs",
      "color": "c5def5",
      "description": "Documentation that moves with a contract"
    },
    {
      "name": "prj:PRJ-0000",
      "color": "6a737d",
      "description": "Usable surface and honest admin writes (complete)"
    },
    {
      "name": "prj:PRJ-0001",
      "color": "0e8a16",
      "description": "Durable broker and remote pull-runner (active)"
    },
    {
      "name": "stage:U0",
      "color": "d93f0b",
      "description": "MS-0007 Configuration integrity and the admin write path"
    },
    {
      "name": "stage:U1",
      "color": "fbca04",
      "description": "MS-0008 One vocabulary, in the page that ships today"
    },
    {
      "name": "stage:U2",
      "color": "1d76db",
      "description": "MS-0009 A build step, recorded and reversible"
    },
    {
      "name": "stage:U3",
      "color": "1d76db",
      "description": "MS-0010 The rebuilt console at parity, then default"
    },
    {
      "name": "stage:U4",
      "color": "1d76db",
      "description": "MS-0011 Legacy retirement and the standing rule"
    },
    {
      "name": "area:ui",
      "color": "c5def5",
      "description": "The console: layout, vocabulary, build"
    },
    {
      "name": "area:identity",
      "color": "c5def5",
      "description": "Actors, roles, capability authorization"
    },
    {
      "name": "area:config-integrity",
      "color": "c5def5",
      "description": "Configuration writers, validation, refusals"
    },
    {
      "name": "stage:U5",
      "color": "b60205",
      "description": "MS-0012 Review findings"
    },
    {
      "name": "C0",
      "color": "fbca04",
      "description": "Founder console: truthful identity and state"
    },
    {
      "name": "C1",
      "color": "fbca04",
      "description": "Chat, mission pipeline, and progress"
    },
    {
      "name": "C2",
      "color": "fbca04",
      "description": "Codex coordinator and maximum models"
    }
  ],
  "milestones": [
    {
      "recordId": "MS-0007",
      "title": "U0 — Configuration integrity and the admin write path",
      "due_on": "2026-08-27T23:59:59Z",
      "state": "closed",
      "description": "MS-0007 · record: plan/MS-0007.json · PRJ-0000 · stage U0\n\nOutcome: Every writer of a configuration file refuses input it cannot represent instead of coercing it and answering 200. The domain-capability registry loses its one-way door: a file in the shipped shape stays readable after the first save, and a user identifier absent from the user registry is refused rather than accepted. Capability authorization on mission bindings is enforced in every identity mode, with the development allowance stated in the auth policy rather than implied by a mode branch. Every configuration writer validates against a registered schema before it writes. The only writer of the capability registry has tests.\n\nNot in scope: No interface change of any kind, no build step, no framework. No renaming of any wire field, configuration key, environment variable or module-level function: snapshotProfile, runtimeProfile, the runtime-profile endpoint, STEEL_MISSION_RUNTIME_PROFILE and resolve_runtime_profile keep their names, because the runtime-profile tests pin them and the harness silently stops patching a renamed function rather than failing. No schema work outside the configuration directory.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0008",
      "title": "U1 — One vocabulary, in the page that ships today",
      "due_on": "2026-09-02T23:59:59Z",
      "state": "closed",
      "description": "MS-0008 · record: plan/MS-0008.json · PRJ-0000 · stage U1\n\nOutcome: Profile, Role and Capability each name exactly one thing in visible text. The capability list is derived from the registry the server already publishes rather than a hardcoded array, and a label prints its key once instead of twice. The picker that chooses which model executes the Delivery Coordinator says so, rather than being labelled Profile beside a list of thirteen capabilities it has nothing to do with. The definition of a Domain Capability is visible to publishers and users, not only behind the owner gate. The work-mode switch states what it changes.\n\nNot in scope: No build step, no framework, no bundler: this milestone edits the shipped page in place, which is what makes it cheap and immediately visible. No restructuring of the eight-section settings dialog, which is MS-0010. No wire renames - the boundary stated in MS-0007 holds here unchanged, because this is a presentation vocabulary and every field, endpoint and environment variable keeps its name.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0009",
      "title": "U2 — A build step, recorded and reversible",
      "due_on": "2026-09-10T23:59:59Z",
      "state": "closed",
      "description": "MS-0009 · record: plan/MS-0009.json · PRJ-0000 · stage U2\n\nOutcome: The front end builds from typed source with a pinned three-package toolchain. The built page is committed, and continuous integration proves it is byte-identical to a clean rebuild, so a hand-edited bundle cannot enter the tree. The new application is reachable behind a server flag at a non-default route. The contract test from MS-0008 passes against both pages.\n\nNot in scope: The default page does not change: the root route still serves the page that ships today, and that file is left byte-untouched so the existing implementation-pinning assertions stay green. No container change and no new runtime dependency in the image. No static-asset route in the server - the served page remains the single file the server already reads, and the closed route allow-list is preserved. No feature work in the new application beyond what the contract test requires.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0010",
      "title": "U3 — The rebuilt console at parity, then default",
      "due_on": "2026-09-30T23:59:59Z",
      "state": "closed",
      "description": "MS-0010 · record: plan/MS-0010.json · PRJ-0000 · stage U3\n\nOutcome: Settings are navigable structures rather than one dialog holding eight sections whose element identifiers are all named after profiles. The capability workspace renders for the roles it was written for, having previously rendered for nobody. The owner assignment editor exists, is authorized, and is the interface for the endpoint that had none. The rebuilt application becomes the default page.\n\nNot in scope: No new product capability and no change to any interface contract: every call the new application makes exists today, and the twelve non-coordinator Domain Capabilities do not become conversational agents. The page that ships today remains in the tree and remains reachable. No visual redesign beyond what the information architecture and the named defects require.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0011",
      "title": "U4 — Legacy retirement and the standing rule",
      "due_on": "2026-10-04T23:59:59Z",
      "state": "closed",
      "description": "MS-0011 · record: plan/MS-0011.json · PRJ-0000 · stage U4\n\nOutcome: The page that shipped before the rebuild is removed as its own recorded decision, and the workplan carries the standing rule for front-end dependencies so the next person does not relitigate it. PRJ-0001 returns to ACTIVE with its milestone dates re-set from the date it resumes.\n\nNot in scope: Nothing else. A removal that also changes something is a removal whose regression cannot be attributed. No new dependency, no visual change, no behaviour change.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0012",
      "title": "U5 — Review findings",
      "due_on": "2026-08-22T23:59:59Z",
      "state": "closed",
      "description": "MS-0012 · record: plan/MS-0012.json · PRJ-0000 · stage U5\n\nOutcome: The four findings from the review of PRJ-0000 are closed. The published image carries no front-end toolchain under /workspace, so the reversibility condition that the image gains no node dependency for the product is true rather than intended. The provider command-line tools under /usr/local/lib legitimately ship node and their own modules, and are untouched. Every one of the thirteen domain capabilities is either assigned or recorded as deliberately unowned with a reason, so a gap is a decision rather than an oversight. Running the product does not modify configuration files that are under version control, so development and testing stop dirtying the tree. A loopback console session with no locally stored identity resolves to a real registered actor rather than an anonymous one that matches nobody and holds no capabilities, so opening the console for the first time is never a dead end with no way to reach Settings.\n\nNot in scope: No new product capability, no interface redesign, no change to the vocabulary or the information architecture settled in MS-0008 and MS-0010. No change to the build toolchain itself: the three pinned packages, the committed page and the drift gate all stand. This milestone closes findings, and a finding that turns out to need design is raised rather than absorbed here. No login screen or identity-picker UI, and no change to oidc-required behaviour: the fix stays inside the development-local loopback trust boundary that already exists.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0001",
      "title": "P0 — Toolchain and CI baseline",
      "due_on": "2026-08-27T23:59:59Z",
      "description": "MS-0001 · record: plan/MS-0001.json.\n\nOutcome: every dependency and runtime jump currently open is landed or explicitly rejected with the full suite green on each, CI compiles every entrypoint rather than four of them, and the test harness has the markers and fixtures the later phases need.\n\nNot in scope: no durable store, no runner protocol, no orchestration change, no renaming.\n\nThe five-state milestone model (PLANNED, ACTIVE, AT_RISK, COMPLETE, ABANDONED) lives in the record. This GitHub milestone is a projection of it. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification.\n\nTarget reset to 2026-08-27 from the 2026-08-20 PRJ-0001 resume date, preserving the original milestone cadence; schedule delta +1 calendar day."
    },
    {
      "recordId": "MS-0013",
      "title": "C0 — Founder console: truthful identity and state",
      "due_on": "2026-09-03T23:59:59Z",
      "description": "MS-0013 · record: plan/MS-0013.json · PRJ-0001 · phase C0\n\nOutcome: Landing on the console as the registered owner shows the true capability state: owner and admin derive their Domain Capabilities from the workspace grant rather than from the raw per-user assignment field that is empty for them by design, so Morgan Vale sees all thirteen instead of an empty-state message. A declared identity that resolves to no registered user falls back the same way a missing one does instead of producing a capability-less actor. The sidebar reports real server and authority state from the health and control-plane readiness endpoints, and a provider status strip shows, per provider, connected, online, idle or working, with token usage where the provider reports it.\n\nNot in scope: No new identity and no new organization: the Founder lands as the existing registered owner, and the shipped Northstar Forge demo company is untouched. No login screen, no identity picker, no change to oidc-required behaviour. No chat surface - that is MS-0014. No new provider - that is MS-0015.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0014",
      "title": "C1 — Chat, mission pipeline, and progress",
      "due_on": "2026-09-17T23:59:59Z",
      "description": "MS-0014 · record: plan/MS-0014.json · PRJ-0001 · phase C1\n\nOutcome: The console has a chat surface wired to the existing chat pipeline, with a normal mode carrying a model picker and a Domain-Capabilities mode carrying the delivery workspace. The four-stage mission pipeline runs as a tooling-level bench against granted missions: plan by Claude at Opus 5 or above, develop and commit by the local model in an isolated worktree, a bounded review loop by Codex, and final review, approval and merge by Claude at Opus 5 or above, under machine accounts with auto-merge armed and the definition of done checked mechanically. A session status feed in the draft job-protocol-v2 shape drives a read-only progress panel, and the coordinator answers what needs the Person from pending-decision state. When a plan proves unclean the mission escalates through the existing user-decision functionality and waits.\n\nNot in scope: No broker or store change - that is MS-0002. The bench implementation is disposable by declaration under D7: it retires when MS-0002 and MS-0003 land, and only the status feed format survives. No new coordinator provider - that is MS-0015. The local model is assigned the smallest mechanical issues first; its weakness relative to the other two workers is a stated operating constraint, not a discovery to be made in review.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0015",
      "title": "C2 — Codex coordinator and maximum models",
      "due_on": "2026-09-24T23:59:59Z",
      "description": "MS-0015 · record: plan/MS-0015.json · PRJ-0001 · phase C2\n\nOutcome: Codex is a first-class coordinator provider, selectable in chat and the default in normal mode. The model role registry's model id and effort flow into the actual adapter invocations, so changing the registry changes the call: the planner and acceptance roles run at Opus 5 or above, and Codex runs at its maximum available model and reasoning setting. The local model provider re-warms on keep-alive expiry instead of refusing, and token usage is captured and surfaced in the status strip.\n\nNot in scope: No fourth provider and no HTTP-endpoint configurability in the registry. No change to the pipeline role assignments themselves - the registry governs who plans, codes, reviews and accepts, and it already says so.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0002",
      "title": "P1 — Durable broker service",
      "due_on": "2026-10-15T23:59:59Z",
      "description": "MS-0002 · record: plan/MS-0002.json · PRJ-0001 · phase P1\n\nOutcome: Broker state lives in one transactional store behind one interface, with a SQLite backend by default and a PostgreSQL backend available. Concurrent writers are serialized rather than overwriting each other. Every lease grant carries a fence token, expiry is swept rather than noticed lazily on the next read, and a daemon drives workflows so that killing the coordinator mid-job resumes the work instead of stranding it. The store also persists the agent-session status feed in the shape the C1 bench established, so the pipeline's progress state survives a restart the same way jobs do.\n\nNot in scope: No remote runner and no gRPC surface; node invocation stays on the local subprocess path. No connector inbox or outbox. No change to mission orchestration in the chat server beyond routing its reads through the store. The legacy broker-state JSON document is still mirrored and is not retired in this milestone.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0003",
      "title": "P2 — Pull-runner agent and job protocol v2",
      "due_on": "2026-11-12T23:59:59Z",
      "description": "MS-0003 · record: plan/MS-0003.json · PRJ-0001 · phase P2\n\nOutcome: A runner process inside customer infrastructure dials out over mutually authenticated TLS, claims jobs from the broker, materializes its own source from a pinned commit or a bundle digest rather than a host-local path, executes in an image pinned by digest, and returns a result signed with its own key and bound to the job, the runner, the image and the inputs. Secrets are carried as references and resolved just in time, never as values in a job specification. The runner gains an agent executor: a job class that runs a coding agent in an isolated worktree with a commit step, machine-account git identity, scoped credentials, a network policy that admits exactly the model endpoints the job declares, and timeout ceilings sized for agent runs. The four-stage pipeline the C1 bench rehearsed runs through the broker and runner, and the bench retires.\n\nNot in scope: No change to the local subprocess execution path, which stays the default so the existing suite keeps exercising it. No connector work. No mission orchestration change. Multi-tenant runner pools and regional placement are later phases; the schema must not preclude them.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification."
    },
    {
      "recordId": "MS-0004",
      "title": "P3 — Durable connector inbox and outbox",
      "due_on": "2026-11-26T23:59:59Z",
      "description": "MS-0004 · record: plan/MS-0004.json.\n\nOutcome: signed inbound events land in a transactional inbox where a unique deduplication key decides which single transaction starts a mission. Outbound connector actions are written in the same transaction as the evidence they record, and delivered with backoff, jitter, honoured retry hints and an attempt cap that ends in a dead-letter queue rather than a dropped exception.\n\nNot in scope: no change to signature verification, no new connectors. Exactly-once reply is effective, not absolute — it rests on connector-specific markers and degrades visibly where a connector has none.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification.\n\nTarget reset to 2026-10-29 from the 2026-08-20 PRJ-0001 resume date, preserving the original milestone cadence; schedule delta +1 calendar day."
    },
    {
      "recordId": "MS-0005",
      "title": "P4 — Single orchestration path",
      "due_on": "2026-12-10T23:59:59Z",
      "description": "MS-0005 · record: plan/MS-0005.json.\n\nOutcome: one orchestration path. Mission control becomes a client of the broker: the chat server runs an embedded control-plane runner executing the existing node bodies unchanged, delivery steps become sandbox jobs, and an approval gate is a first-class waiting state on a job instead of a thread that exits and is relaunched. A restart resumes missions rather than marking them paused.\n\nNot in scope: no new mission semantics; node lists translate to a sequential chain. Mission documents and the integrity chain stay authoritative.\n\nThe test churn of the whole project concentrates here. The chat server is loaded by path and monkeypatched at module scope: wrap, never rename.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification.\n\nTarget reset to 2026-11-19 from the 2026-08-20 PRJ-0001 resume date, preserving the original milestone cadence; schedule delta +1 calendar day."
    },
    {
      "recordId": "MS-0006",
      "title": "P5 — Steel-Mission naming",
      "due_on": "2026-12-23T23:59:59Z",
      "description": "MS-0006 · record: plan/MS-0006.json.\n\nOutcome: the repository names one product. Executable names, environment variable prefixes, schema identifier namespaces and prose all read Steel-Mission.\n\nNot in scope: no behaviour change of any kind. A rename that also fixes something is a rename whose regression cannot be attributed.\n\nSequenced last: this milestone collides with the file paths of every other phase. The one exception is the package name, settled up front by decision PRJ-0001-D4 so that P1 never creates a package P5 must rename.\n\nThis GitHub milestone is a projection of the record. Closing it means the tasks are closed or explicitly dropped — never that anything passed verification.\n\nTarget reset to 2026-12-03 from the 2026-08-20 PRJ-0001 resume date, preserving the original milestone cadence; schedule delta +1 calendar day."
    }
  ],
  "epics": [
    {
      "key": "epic-p0",
      "title": "P0 — Toolchain and CI baseline",
      "milestone": "MS-0001",
      "labels": [
        "epic",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "outcome": "Every dependency and runtime jump currently open is landed or explicitly rejected with the full suite green on each. Continuous integration compiles every entrypoint rather than four of them. The test harness has the markers, the requirements split and the fixtures that P1 through P4 need before any of them start writing tests.",
      "notInScope": "No durable store, no runner protocol, no orchestration change, no renaming. This milestone touches build, test and dependency surfaces only.",
      "done": "The four open dependency pull requests are each merged or closed with a written reason. `make release-check` and the CI compile step cover the same six entrypoints. `python -m pytest` reports the full suite green on the supported interpreter matrix."
    },
    {
      "key": "epic-p1",
      "title": "P1 — Durable broker service",
      "milestone": "MS-0002",
      "labels": [
        "epic",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "outcome": "Broker state lives in one transactional store behind one interface, with a SQLite backend by default and a PostgreSQL backend available. Concurrent writers are serialized rather than overwriting each other. Every lease grant carries a fence token, expiry is swept rather than noticed lazily on the next read, and a daemon drives workflows so that killing the coordinator mid-job resumes the work instead of stranding it.",
      "notInScope": "No remote runner and no gRPC surface; node invocation stays on the local subprocess path. No connector inbox or outbox. No change to mission orchestration beyond routing its reads through the store. The legacy JSON state document is still mirrored and is not retired in this milestone.",
      "done": "The full existing suite passes at every step with the mirror on, proving the storage swap changed no observable behaviour. New unit tests cover the store and the state machine. The coordinator-kill test passes. The parity suite passes against both backends."
    },
    {
      "key": "epic-p2",
      "title": "P2 — Pull-runner agent and job protocol v2",
      "milestone": "MS-0003",
      "labels": [
        "epic",
        "P2",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "outcome": "A runner process inside customer infrastructure dials out over mutual TLS, claims jobs from the broker, materializes its own source from a pinned commit or bundle digest, executes in a digest-pinned image, and returns a result signed with its own key and bound to the job, the runner, the image and the inputs. Secrets are carried as references and resolved just in time.",
      "notInScope": "No change to the local subprocess execution path, which stays the default so the existing suite keeps exercising it. No connector work. No mission orchestration change. Multi-tenant runner pools and regional placement are later phases; the schema must not preclude them.",
      "done": "An end-to-end test drives the daemon and two agents over loopback mutual TLS to a signed result. The runner-death, secret-materialization and result-binding acceptance tests pass. The sandbox extraction is covered by an argument characterization test."
    },
    {
      "key": "epic-p3",
      "title": "P3 — Durable connector inbox and outbox",
      "milestone": "MS-0004",
      "labels": [
        "epic",
        "P3",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "outcome": "Signed inbound events land in a transactional inbox where a unique deduplication key decides which single transaction starts a mission. Outbound connector actions are written to an outbox in the same transaction as the evidence they record, and delivered by a worker with exponential backoff, jitter, honoured retry hints and an attempt cap that ends in a dead-letter queue.",
      "notInScope": "No change to signature verification on ingress. No new connectors. Exactly-once reply is effective, not absolute, and degrades visibly where a connector cannot be queried for a marker.",
      "done": "The duplicate-webhook and connector-retry acceptance tests pass, including the crash variant. Receipt-file assertions are rewritten to inbox-row assertions, and that rewrite is stated in the pull request rather than absorbed."
    },
    {
      "key": "epic-p4",
      "title": "P4 — Single orchestration path",
      "milestone": "MS-0005",
      "labels": [
        "epic",
        "P4",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "outcome": "There is one orchestration path. Mission control becomes a client of the broker rather than a parallel engine, approval gates are a first-class waiting state on a job, and a restart resumes missions rather than marking them paused.",
      "notInScope": "No new mission semantics. Node lists translate to a sequential chain, preserving today's ordering. Mission documents and the integrity chain stay authoritative and are referenced by digest.",
      "done": "The per-mission daemon threads are deleted, not bypassed. The startup supervision that marked missions paused is gone along with the field it set. The backup-and-restore acceptance test passes. The rollout flag is removed at the end of the milestone."
    },
    {
      "key": "epic-p5",
      "title": "P5 — Steel-Mission naming",
      "milestone": "MS-0006",
      "labels": [
        "epic",
        "P5",
        "area:naming",
        "prj:PRJ-0001"
      ],
      "outcome": "The repository names one product. Executable names, environment variable prefixes, schema identifier namespaces and prose all read Steel-Mission, with no residual reference to the separate programme this code was extracted from.",
      "notInScope": "No behaviour change of any kind. This milestone renames and shims only.",
      "done": "A search of the tracked tree for the prior product name returns only the migration provenance record and the compatibility shim table, both of which name it deliberately. The suite passes before and after each rename step, and every removed shim is its own recorded decision."
    },
    {
      "key": "epic-u0",
      "title": "U0 — Configuration integrity and the admin write path",
      "milestone": "MS-0007",
      "labels": [
        "epic",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "outcome": "Every writer of a configuration file refuses input it cannot represent instead of coercing it and answering 200. The capability registry loses its one-way door. Capability authorization is enforced in every identity mode. Every configuration writer validates against a registered schema before it writes.",
      "notInScope": "No interface change, no build step, no framework. No renaming of any wire field, configuration key, environment variable or module-level function — the runtime-profile tests pin those names and the harness stops patching a renamed function without failing.",
      "done": "A test exists for each refusal and was watched to fail without the change. The capability-registry endpoint has authorization and round-trip tests. Every configuration writer validates against a registered schema. Full suite green on both interpreters."
    },
    {
      "key": "epic-u1",
      "title": "U1 — One vocabulary, in the page that ships today",
      "milestone": "MS-0008",
      "labels": [
        "epic",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "outcome": "Profile, Role and Capability each name exactly one thing in visible text. Labels are derived from the registry the server publishes rather than hardcoded, and a label prints its key once. The definition of a Domain Capability is visible to the people who are assigned capabilities.",
      "notInScope": "No build step, no framework, no bundler — this edits the page that ships today. No restructuring of the settings dialog, which is U3. No wire renames: this is a presentation vocabulary and every field, endpoint and environment variable keeps its name.",
      "done": "A behavioural contract test is green against the shipped page, with every assertion watched to fail first. A lexicon test refuses the banned collocations."
    },
    {
      "key": "epic-u2",
      "title": "U2 — A build step, recorded and reversible",
      "milestone": "MS-0009",
      "labels": [
        "epic",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "outcome": "The front end builds from typed source with a pinned three-package toolchain. The built page is committed and continuous integration proves it is a clean rebuild. The new application is reachable behind a server flag at a non-default route.",
      "notInScope": "The default page does not change and the page that ships today is byte-untouched, so the existing implementation-pinning assertions stay green. No container change, no new runtime dependency in the image, and no static-asset route in the server.",
      "done": "A clean checkout rebuilds to a byte-identical committed page. The existing script-parse test still passes against the built output. The dependency audit is clean. Reverting the build commit leaves a working product, demonstrated."
    },
    {
      "key": "epic-u3",
      "title": "U3 — The rebuilt console at parity, then default",
      "milestone": "MS-0010",
      "labels": [
        "epic",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "outcome": "Settings are navigable structures rather than one dialog of eight sections. The capability workspace renders for the roles it was written for. The owner assignment editor exists and is the interface for the endpoint that had none. The rebuilt application becomes the default.",
      "notInScope": "No new product capability and no interface contract change: every call the new application makes exists today, and the twelve non-coordinator capabilities do not become conversational agents. The superseded page stays in the tree and reachable. No visual redesign beyond what the information architecture and the named defects require.",
      "done": "The contract test covers each restored behaviour, watched to fail first. The 231 implementation pins are removed in the flip pull request with the contract test already green for two milestones. The container comes up and the shipped demo organisation is reachable."
    },
    {
      "key": "epic-u4",
      "title": "U4 — Legacy retirement and the standing rule",
      "milestone": "MS-0011",
      "labels": [
        "epic",
        "prj:PRJ-0000",
        "stage:U4",
        "area:ui"
      ],
      "outcome": "The page that shipped before the rebuild is removed as its own recorded decision, the workplan carries the standing rule for front-end dependencies, and PRJ-0001 returns to active with dates re-set from the day it resumes.",
      "notInScope": "Nothing else. A removal that also changes something is a removal whose regression cannot be attributed.",
      "done": "The removal names the decision that authorises it and states how it is undone. The suite is green before and after. PRJ-0001 reads active and its estimate is re-defended with the delta printed."
    },
    {
      "key": "epic-u5",
      "title": "U5 — Review findings",
      "milestone": "MS-0012",
      "labels": [
        "epic",
        "prj:PRJ-0000",
        "stage:U5",
        "area:ci"
      ],
      "outcome": "The four findings from the review of PRJ-0000 are closed: the published image carries no front-end toolchain under /workspace, every domain capability is either assigned or recorded as deliberately unowned with a reason, running the product no longer modifies version-controlled configuration, and a fresh loopback console session resolves to a real registered actor rather than a capability-less synthetic identity.",
      "notInScope": "No new product capability, no interface redesign, no change to the vocabulary or information architecture, and no change to the build toolchain itself. No login screen or identity picker, and no change to oidc-required behaviour: the identity fix stays inside the existing development-local loopback trust boundary. A finding that turns out to need design is raised rather than absorbed here.",
      "done": "The image is rebuilt and inspected with the size difference recorded, and a strengthened guard test fails if a future image would carry the front-end toolchain under /workspace. Capability ownership or a deliberate exception with a stated reason is asserted by a test. Running the server against a clean checkout leaves the tree clean, demonstrated. A headerless loopback call to /api/auth/whoami resolves the active organization's registered owner by role and can reach Settings -> Users, while oidc-required and non-loopback behaviour remain unchanged."
    },
    {
      "key": "epic-c0",
      "title": "C0 — Founder console: truthful identity and state",
      "milestone": "MS-0013",
      "labels": [
        "epic",
        "C0",
        "area:identity",
        "prj:PRJ-0001"
      ],
      "outcome": "Landing on the console as the registered owner shows the true capability state: owner and admin derive their Domain Capabilities from the workspace grant rather than from the raw per-user assignment field that is empty for them by design, so Morgan Vale sees all thirteen instead of an empty-state message. A declared identity that resolves to no registered user falls back the same way a missing one does instead of producing a capability-less actor. The sidebar reports real server and authority state from the health and control-plane readiness endpoints, and a provider status strip shows, per provider, connected, online, idle or working, with token usage where the provider reports it.",
      "notInScope": "No new identity and no new organization: the Founder lands as the existing registered owner, and the shipped Northstar Forge demo company is untouched. No login screen, no identity picker, no change to oidc-required behaviour. No chat surface - that is MS-0014. No new provider - that is MS-0015.",
      "done": "A fresh incognito session against a clean checkout lands as the registered owner with thirteen capabilities visible, demonstrated against the running server. A request carrying a stale X-Present-Actor naming an unregistered user resolves to the registered owner rather than a ghost, asserted by a test watched to fail first. Stopping and starting the local model observably changes the status strip, and the sidebar no longer contains hardcoded state strings, proven by a test that greps the built page."
    },
    {
      "key": "epic-c1",
      "title": "C1 — Chat, mission pipeline, and progress",
      "milestone": "MS-0014",
      "labels": [
        "epic",
        "C1",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "outcome": "The console has a chat surface wired to the existing chat pipeline, with a normal mode carrying a model picker and a Domain-Capabilities mode carrying the delivery workspace. The four-stage mission pipeline runs as a tooling-level bench against granted missions: plan by Claude at Opus 5 or above, develop and commit by the local model in an isolated worktree, a bounded review loop by Codex, and final review, approval and merge by Claude at Opus 5 or above, under machine accounts with auto-merge armed and the definition of done checked mechanically. A session status feed in the draft job-protocol-v2 shape drives a read-only progress panel, and the coordinator answers what needs the Person from pending-decision state. When a plan proves unclean the mission escalates through the existing user-decision functionality and waits.",
      "notInScope": "No broker or store change - that is MS-0002. The bench implementation is disposable by declaration under D7: it retires when MS-0002 and MS-0003 land, and only the status feed format survives. No new coordinator provider - that is MS-0015. The local model is assigned the smallest mechanical issues first; its weakness relative to the other two workers is a stated operating constraint, not a discovery to be made in review.",
      "done": "A granted rehearsal mission runs the full pipeline unattended on a real issue: the plan is recorded, the local model commits under its machine account, the review loop completes at least one correction round, the acceptance role approves, auto-merge fires on the approval, and the evidence pack exists. A deliberately unclean plan escalates through the decision endpoint and resumes on the Person's decision, asserted by a test. The progress panel renders live sessions from the feed. The full suite is green throughout, and the bench refuses issues labelled security-review."
    },
    {
      "key": "epic-c2",
      "title": "C2 — Codex coordinator and maximum models",
      "milestone": "MS-0015",
      "labels": [
        "epic",
        "C2",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "outcome": "Codex is a first-class coordinator provider, selectable in chat and the default in normal mode. The model role registry's model id and effort flow into the actual adapter invocations, so changing the registry changes the call: the planner and acceptance roles run at Opus 5 or above, and Codex runs at its maximum available model and reasoning setting. The local model provider re-warms on keep-alive expiry instead of refusing, and token usage is captured and surfaced in the status strip.",
      "notInScope": "No fourth provider and no HTTP-endpoint configurability in the registry. No change to the pipeline role assignments themselves - the registry governs who plans, codes, reviews and accepts, and it already says so.",
      "done": "A chat is answered by Codex with the provider and model visible in the status strip. Changing the registry's model id demonstrably changes the invoked model, shown from the invocation record rather than the display. A chat issued more than thirty idle minutes after the last one succeeds without a manual warm-up. The provider enum widening is covered by updated contract tests and schema fixtures, and the full suite is green."
    }
  ],
  "issues": [
    {
      "key": "p0-checkout",
      "title": "Resolve actions/checkout v4 to v7 bump",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "The open checkout action bump is merged, or closed with a written reason recorded in the pull request. Workflows run with repository credentials, so an unreviewed action bump is a real change and is reviewed as one.",
      "acceptance": "Pull request #6 is merged or closed with a reason. The CI matrix is green on the resulting main.",
      "surface": "CI / tooling"
    },
    {
      "key": "p0-setup-python",
      "title": "Resolve actions/setup-python v5 to v7 bump",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "The open setup-python action bump is merged, or closed with a written reason. A two-major-version jump in the action that provisions the interpreter can change which patch release the matrix actually runs; confirm the resolved versions before merging, do not infer them.",
      "acceptance": "Pull request #8 is merged or closed with a reason. The resolved interpreter versions on both matrix legs are recorded in the pull request. The full suite is green on each.",
      "surface": "CI / tooling"
    },
    {
      "key": "p0-deps",
      "title": "Resolve the grouped development dependency bump",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "The open grouped development dependency bump is merged, or closed with a written reason. A grouped bump hides which member broke the suite, so if it fails, split it before diagnosing.",
      "acceptance": "Pull request #9 is merged or closed with a reason. The full suite is green on the resulting main.",
      "surface": "CI / tooling"
    },
    {
      "key": "p0-py314",
      "title": "Decide the private-runner base image Python 3.12 to 3.14 jump",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "The container base image jump is landed or rejected on evidence. The container runtime and the continuous integration interpreter matrix are different runtimes that fail independently: a green matrix says nothing about the container, and a built container says nothing about the matrix. Whichever way this goes, the reason is written down.",
      "acceptance": "The image builds with `make private-runner-image`, `PRESENT_PRIVATE_RUNNER_MODE=docker bin/present-private-runner status` succeeds against it, and a real job runs end to end in the built image — or the bump is closed with the failure recorded. If the image moves to 3.14, the CI matrix gains a leg that runs the suite on 3.14 in the same pull request, or the divergence is stated on the issue.",
      "surface": "CI / tooling",
      "dependsOn": []
    },
    {
      "key": "p0-compile-drift",
      "title": "Compile every entrypoint in CI, not four of them",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "The CI compile step covers the same entrypoints as `make release-check`. Today CI compiles four files and the local gate compiles five, so the private runner and the lease broker can be pushed with a syntax error that only the local gate would have caught. The two lists come from one place so they cannot drift again.",
      "acceptance": "A deliberate syntax error introduced in bin/present-lease-broker fails CI, and is then reverted. `make release-check` and the CI step name the same files.",
      "surface": "CI / tooling"
    },
    {
      "key": "p0-pytest-harness",
      "title": "Add pytest.ini markers and tests/conftest.py",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "A root pytest configuration declares the postgres, docker and slow markers, and a conftest provides the shared fixtures the later phases need: a temporary store, process managers for the daemon and agents, a fake connector server and a crash-point helper. These exist before P1 writes its first test, so that no phase invents its own harness.",
      "acceptance": "`python -m pytest -m 'not postgres and not docker'` selects the default set. The existing 227 tests pass unchanged. `pytest --markers` lists the three markers with descriptions.",
      "surface": "CI / tooling"
    },
    {
      "key": "p0-requirements-split",
      "title": "Split runtime, postgres and development requirements",
      "milestone": "MS-0001",
      "labels": [
        "task",
        "P0",
        "area:ci",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p0",
      "requirement": "Requirements are split into runtime, an optional PostgreSQL extra, and development. The cryptography dependency moves from development to runtime, because P2 signs with it in production. The default SQLite path stays on the standard library plus the runtime file, so an operator who does not want PostgreSQL never installs psycopg.",
      "acceptance": "A clean environment installing only the runtime requirements can run the broker and the worker. The development file includes the runtime file. CI installs both. Installing the PostgreSQL extra is not required for the default suite to pass.",
      "surface": "CI / tooling"
    },
    {
      "key": "p1-store-iface",
      "title": "Store interface, portable DDL and the SQLite backend",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "One storage interface exists with a SQLite backend behind it: transactions, job claim, event append, exclusive state for a task, and a factory that opens a store from a URL. SQLite uses write-ahead logging, immediate-mode write transactions, a busy timeout and a durable commit, so that two processes writing at once serialize rather than corrupt. Table definitions are portable, with the dialect differences confined to one shim.",
      "acceptance": "Unit tests in tests/test_store.py cover: two concurrent writers serialize with no lost update; a claim under contention hands one job to exactly one caller; an event append is visible only after its transaction commits. The existing suite is untouched and green.",
      "surface": "control plane"
    },
    {
      "key": "p1-statemachine",
      "title": "One authoritative state machine with atomic event append",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "A single transition table governs job and workflow states, with one entry point that applies the transition and appends the event row in the same transaction, so an event and the state it describes can never disagree. A compatibility map covers the legacy status set, so nothing that reads the old statuses breaks.",
      "acceptance": "tests/test_statemachine.py enumerates every legal transition and asserts every illegal one is refused. A test asserts that a failed transition leaves no event row behind. Every legacy status maps to exactly one new status, asserted exhaustively rather than by sampling.",
      "surface": "control plane"
    },
    {
      "key": "p1-state-swap",
      "title": "Route broker state through the store behind an environment flag",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "Broker state reads and writes go through the store, with every read-modify-write wrapped in an exclusive state transaction. No command handler is rewritten in this step — the point is to convert last-writer-wins into serialized transactions underneath handlers that do not change, so that any behaviour difference is attributable to the storage swap alone. The legacy JSON document is mirrored after each mutation and auto-migrates on first open.",
      "acceptance": "The full existing suite passes with the flag on and the mirror on, unmodified. A concurrency test runs two broker commands against one state at once and asserts neither update is lost — the same test fails against the pre-change code. Migration from an existing legacy state document round-trips: export, import, compare.",
      "surface": "control plane",
      "dependsOn": [
        "p1-store-iface",
        "p1-statemachine"
      ]
    },
    {
      "key": "p1-transitions-routed",
      "title": "Route every status change through the single transition entry point",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "No code path sets a status directly. Every status change goes through the transition entry point, which is what makes the event log complete rather than mostly complete.",
      "acceptance": "A test greps the source for direct status assignment outside the state machine module and fails on any hit. The full suite is green.",
      "surface": "control plane",
      "dependsOn": [
        "p1-state-swap"
      ]
    },
    {
      "key": "p1-leases-fencing",
      "title": "Leases table, fence tokens and the expiry sweeper",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "Leases live in their own table and the broker is authoritative over grant, heartbeat, expiry and reclaim. Every grant increments a per-job fence token; heartbeats and results carry it and a stale fence is refused. A sweeper expires leases on a timer rather than lazily on the next read, so an expired lease is expired whether or not anyone looked. Heartbeat renewal writes the leases table directly and never inside the exclusive state transaction, because a transaction held across a renewal collapses throughput.",
      "acceptance": "A test grants a lease, expires it, grants again, and asserts the second grant carries fence + 1 and that a heartbeat on the first fence is refused. A test asserts a lease expires with no reader present. A throughput test asserts heartbeat renewal does not block on a concurrent state transaction.",
      "surface": "control plane",
      "dependsOn": [
        "p1-state-swap"
      ]
    },
    {
      "key": "p1-controller",
      "title": "Extract the drive loop into a controller module",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "The distributed run loop moves out of the command handler into a controller: admission, topological ready batches, dispatch, collect, retry. Exponential backoff is added behind configuration whose default reproduces today's single retry exactly, so the change is invisible to existing behaviour until someone turns it on. The distributed run command becomes enqueue-then-drive-synchronously and produces identical output.",
      "acceptance": "The existing distributed run tests pass with byte-identical output. A backoff test with the non-default configuration asserts the delays and the attempt cap. A test asserts the default configuration produces exactly today's retry behaviour.",
      "surface": "control plane",
      "dependsOn": [
        "p1-leases-fencing"
      ]
    },
    {
      "key": "p1-brokerd",
      "title": "Broker daemon: controller loop, leader lease, operations API, graceful drain",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "A daemon drives workflows continuously: the controller loop, the lease sweeper, a controller-singleton leader lease so that two daemons cannot co-drive one workflow, an authenticated HTTP operations API replacing the subprocess-per-request broker server, and a graceful drain on SIGTERM. The operations API reads its bearer token from a 0600 file, matching the existing key conventions. The existing broker server becomes a thin authenticated proxy with its response shapes preserved.",
      "acceptance": "tests/test_broker_daemon.py starts two daemons against one store and asserts exactly one drives. A SIGTERM drain test asserts in-flight work completes and no new work is claimed. The existing broker server tests pass with a token header added — the only test change in this milestone. An unauthenticated request to the operations API is refused.",
      "surface": "control plane",
      "dependsOn": [
        "p1-controller"
      ]
    },
    {
      "key": "p1-schemas",
      "title": "P1 schemas and event types",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:schema",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "The store archive, lease record and daemon status schemas exist with registry entries, and the event type registry gains job claimed, lease expired, lease reclaimed, workflow resumed after restart, controller elected and controller drained. Schemas land before the code that produces them, per the repository's schema-first discipline.",
      "acceptance": "Every new schema validates its own fixtures. Every new event type is emitted by a test and validated against the event schema on insert. The registry entries are appended rather than the file re-sorted, so the diff shows the additions.",
      "surface": "schemas (authority-owned)"
    },
    {
      "key": "p1-postgres",
      "title": "PostgreSQL backend and the store parity suite",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "A PostgreSQL backend passes the same parity suite as SQLite, claiming work by row lock with skip-locked. It is optional: the default path stays on the standard library. A development compose file and a CI service job run the suite against a real server, because a portability layer tested only against one dialect is a portability layer that has not been tested.",
      "acceptance": "`make test-postgres` runs the parity suite green against the compose server. The CI PostgreSQL job is green. The default suite passes with psycopg not installed.",
      "surface": "control plane",
      "dependsOn": [
        "p1-store-iface"
      ]
    },
    {
      "key": "p1-ac-coordinator",
      "title": "Acceptance: coordinator dies mid-job, work resumes with no duplicate side effect",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "acceptance-criterion",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "Killing the coordinator in the middle of a job resumes the work on restart and produces the side effect exactly once. This is the criterion the whole of P1 exists to satisfy; if it does not pass, the durability work is not done regardless of what else is green.",
      "acceptance": "The test SIGKILLs the daemon at an injected crash point, restarts it, and asserts the workflow completes, that the side-effect counter file reads exactly 1, and that exactly one recovery ledger entry exists. Crash points are environment-gated and inert in production.",
      "surface": "control plane",
      "dependsOn": [
        "p1-brokerd"
      ]
    },
    {
      "key": "p1-docs",
      "title": "Document the durable broker in architecture and operations",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:docs",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "The architecture and operations documents describe the store, the daemon, the leader lease, fencing and the operations API, in the same pull requests as the code where the contract moved. An operator can find how to back up, restore and inspect the store without reading the source.",
      "acceptance": "A person following operations.md alone can start the daemon, inspect a workflow, take a backup and restore it.",
      "surface": "docs"
    },
    {
      "key": "p2-schemas",
      "title": "Job specification v2, job result v2 and runner identity schemas",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:schema",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The v2 job specification replaces the host-local workspace path with a source that is a pinned commit, a bundle digest, or none; pins the image by digest; carries the command as an argument vector; carries secrets as references only, never values; and declares an egress profile, resources, an idempotency key, a retry policy, a deadline and its attestation requirements. The v2 result carries the fence token, the runner id, the image digest actually used, the input hashes, the outputs, a log digest, output tails with explicit truncation markers, and a signature with a key id. Truncation is marked because output that stops silently is indistinguishable from output that ended.",
      "acceptance": "Both schemas validate their fixtures and reject a specification carrying a secret value, a specification with an unpinned image, and a result missing its fence token. Registry entries are appended.",
      "surface": "schemas (authority-owned)"
    },
    {
      "key": "p2-proto",
      "title": "Runner protocol definition, checked-in code generation and a drift gate",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The gRPC service definition exists with its generated code checked in, a make target regenerates it, and a check target fails CI if the generated code has drifted from the definition. Message bodies are canonical JSON validated against the v2 schemas; the protocol buffer is framing only and never becomes the schema of record.",
      "acceptance": "`make proto` regenerates identical output on a clean tree. `make proto-check` fails on a hand-edited generated file, and is wired into CI. A test asserts that a message body failing schema validation is refused before it reaches a handler.",
      "surface": "schemas (authority-owned)",
      "dependsOn": [
        "p2-schemas"
      ]
    },
    {
      "key": "p2-sandbox-extract",
      "title": "Extract the container hardening logic into an importable module",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The container hardening logic moves out of the private runner executable into an importable module, and the executable becomes a thin command-line wrapper over it. Its v1 schema and every one of its existing tests are unchanged. This is a pure refactor and ships alone: a regression here weakens an isolation boundary without failing anything, which is why it gets its own pull request and a characterization test rather than being folded into the gateway work.",
      "acceptance": "A characterization test captures the container arguments produced by the pre-change code for a set of representative jobs and asserts the post-change code produces them byte-identically. Every existing private runner test passes unmodified. The pull request contains this change and nothing else.",
      "surface": "subprocess or container execution"
    },
    {
      "key": "p2-runner-registry",
      "title": "Runner registry, enrolment and approval",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "A runner enrols, presenting its labels and its result-signing public key, and does no work until a person approves it. The public key is pinned at enrolment, so a runner that later presents a different key is a different runner. Shared-secret identity is not extended to remote runners.",
      "acceptance": "An unapproved runner is refused work. An approved runner claims. A result signed by a key that is not the pinned key is rejected. Revoking a runner takes effect on its next claim, and the test asserts the in-flight lease is handled rather than silently orphaned.",
      "surface": "control plane"
    },
    {
      "key": "p2-gateway",
      "title": "gRPC gateway service, behind a configuration flag",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The daemon exposes a runner-facing gRPC gateway that maps a peer certificate to a runner row and performs claim, heartbeat and result inside store transactions. Streams hold no durable truth: everything lives in the database, a hello re-associates a runner after a reconnect, and leases outlive stream death until their time to live expires. The listener is off by default until the agent exists.",
      "acceptance": "tests/test_runner_protocol.py kills a stream mid-job and asserts the runner re-associates on reconnect and keeps its lease. A heartbeat carrying a stale fence receives the stale-fence refusal rather than an extension. An unauthenticated peer is refused at the transport, not in a handler.",
      "surface": "a network-listening service, or its bind address",
      "dependsOn": [
        "p2-proto",
        "p2-runner-registry",
        "p1-leases-fencing"
      ]
    },
    {
      "key": "p2-agent",
      "title": "Pull-runner agent: claim loop, source materialization, signing",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The agent dials out over mutual TLS, claims work, materializes its own source from a pinned commit or a bundle digest, resolves secret references just in time into the sandboxed process environment, executes through the extracted sandbox module, streams artifacts in chunks rather than base64 over standard input and output, and signs its result. It reports the image digest the container runtime actually used, not the one it was asked for.",
      "acceptance": "tests/test_runner_agent.py drives a job end to end against a loopback gateway. A test asserts source materialization refuses a commit that does not match the requested digest. A test asserts artifact streaming handles a payload larger than the old truncation limit without truncating.",
      "surface": "subprocess or container execution",
      "dependsOn": [
        "p2-gateway",
        "p2-sandbox-extract"
      ]
    },
    {
      "key": "p2-mtls-ca",
      "title": "Mutual TLS configuration and a development certificate authority helper",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The gateway and the agent authenticate each other with mutual TLS, configured through a daemon configuration block. A development helper issues a local certificate authority and leaf certificates, built on the cryptography dependency already present. The helper is explicitly for development and says so where an operator will read it, so that nobody ships its output.",
      "acceptance": "The end-to-end test runs over loopback mutual TLS with certificates from the helper. A client presenting no certificate, an expired certificate, or one from a different authority is refused in three separate assertions. The documentation states plainly that the helper's output is not for production.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "p2-gateway"
      ]
    },
    {
      "key": "p2-transport-branch",
      "title": "Branch the worker transport seam to the remote runner path",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The dormant transport-kind seam branches: the remote runner kind routes node invocation to a job-table enqueue and a wait on the result. The local subprocess kind stays the default and is not touched, so the whole existing suite keeps exercising the unchanged path. The remote artifact store stub becomes real.",
      "acceptance": "An end-to-end test runs the daemon and two agent processes over loopback mutual TLS and completes a workflow. The full existing suite passes on the default transport, unmodified.",
      "surface": "worker",
      "dependsOn": [
        "p2-agent"
      ]
    },
    {
      "key": "p2-ac-runner-death",
      "title": "Acceptance: runner dies, the lease is reclaimed and the stale result is refused",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "acceptance-criterion",
        "area:runner",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "A runner that dies mid-job has its lease reclaimed by another runner, and if the dead runner comes back and replays its old result, that result is refused. Reclaim without fencing is not safety; it is a second writer.",
      "acceptance": "The test SIGKILLs agent A with a short lease time to live, asserts agent B receives the job with fence + 1, and asserts that replaying agent A's result is rejected as a stale fence rather than accepted or silently ignored.",
      "surface": "control plane",
      "dependsOn": [
        "p2-transport-branch"
      ]
    },
    {
      "key": "p2-ac-secrets",
      "title": "Acceptance: no secret appears in arguments, logs, store or artifacts",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "acceptance-criterion",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "A secret resolved for a job never appears in process arguments, in the store, in the logs, or in an artifact. The test is a grep for a sentinel across all four, and any path added later that could carry a secret is added to it.",
      "acceptance": "A container shim captures the process arguments. The test greps the arguments, a dump of the store, the log files and the produced artifacts for the sentinel and asserts zero hits. The test is watched to fail with the sentinel deliberately leaked.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "p2-agent"
      ]
    },
    {
      "key": "p2-ac-binding",
      "title": "Acceptance: a result is bound to job, runner, image and inputs",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "acceptance-criterion",
        "area:runner",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "A result is only accepted if it is bound to the job it was issued for, the runner that claimed it, the image that actually ran, and the inputs that were supplied. A signature that proves who sent a result, without binding what it describes, proves very little.",
      "acceptance": "Three separate assertions: a result with a tampered image digest is rejected; a result with tampered input hashes is rejected; a result signed by an unenrolled key is rejected. Each is watched to pass before the check exists and fail after, confirming the test discriminates.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "p2-agent"
      ]
    },
    {
      "key": "p2-docs",
      "title": "Document the runner protocol, enrolment and operating a remote runner",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:docs",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p2",
      "requirement": "The control-plane protocol document gains the v2 surfaces, the security policy moves remote scheduling out of what is not yet included, and the operations document explains enrolling, approving, revoking and operating a runner in someone else's infrastructure.",
      "acceptance": "A person following the documentation alone can enrol a runner against a local daemon and run a job on it.",
      "surface": "docs"
    },
    {
      "key": "p3-tables",
      "title": "Inbox and outbox tables and modules, unwired",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "The inbox and outbox tables and their modules exist and are tested in isolation before anything is wired to them, so that a failure during the swap is attributable to the swap. The outbox delivery worker does exponential backoff with jitter, honours a retry hint on 429 and 503, and caps attempts into a dead-letter queue.",
      "acceptance": "tests/test_inbox_outbox.py covers deduplication under concurrency, the backoff schedule including jitter bounds, retry-hint precedence over the computed backoff, and the transition into the dead-letter queue at the attempt cap. Nothing in the chat server calls these yet.",
      "surface": "control plane"
    },
    {
      "key": "p3-ingress-swap",
      "title": "Replace receipt-file ingress deduplication with a transactional insert",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "area:connector",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "Workflow ingress deduplicates by a unique key in one transaction that either inserts or does nothing, and the mission starts only for the transaction that inserted and claimed the row. This replaces a receipt file plus a lock released before the work begins, which is a race whose window is exactly the time it takes to start a mission. Signature verification is unchanged.",
      "acceptance": "Tests that asserted on receipt files are rewritten to assert on inbox rows, and the rewrite is called out in the pull request rather than absorbed. The full suite is green. Signature-rejection tests are untouched and still pass.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "p3-tables"
      ]
    },
    {
      "key": "p3-outbox-github",
      "title": "Route GitHub connector delivery through the outbox",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "GitHub connector actions are enqueued to the outbox in the same transaction as the evidence write, and delivered by the worker. An idempotency marker is embedded in the body so that an in-flight row can be verified by query after a restart before anything is re-sent. GitHub ships first precisely because its marker can be read back.",
      "acceptance": "A fake-endpoint test asserts one delivery for one enqueue across a retry. A restart test with an in-flight row asserts the marker query runs and no second message is sent. The bare exception that dropped delivery failures is gone.",
      "surface": "control plane",
      "dependsOn": [
        "p3-tables"
      ]
    },
    {
      "key": "p3-outbox-slack",
      "title": "Route Slack connector delivery through the outbox",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "Slack connector actions go through the outbox on the same pattern, using message metadata as the idempotency marker. Where the marker cannot be read back reliably, the behaviour degrades to a retry with a visible duplicate annotation, and the documentation says which of the two applies.",
      "acceptance": "A fake-endpoint test covers delivery, retry and the dead-letter path. The documentation states the actual guarantee for this connector, not the aspiration.",
      "surface": "control plane",
      "dependsOn": [
        "p3-outbox-github"
      ]
    },
    {
      "key": "p3-outbox-jira",
      "title": "Route Jira connector delivery through the outbox",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "Jira connector actions go through the outbox on the same pattern, using an entity property as the idempotency marker, with the same honest degradation rule where it cannot be read back.",
      "acceptance": "A fake-endpoint test covers delivery, retry and the dead-letter path. The documentation states the actual guarantee for this connector.",
      "surface": "control plane",
      "dependsOn": [
        "p3-outbox-github"
      ]
    },
    {
      "key": "p3-dlq-cli",
      "title": "Dead-letter queue listing and requeue",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "An operator can list the dead-letter queue and requeue an entry, with the reason for each entry recorded. A dead letter nobody can see is a dropped message with extra steps.",
      "acceptance": "The list and requeue commands are covered by tests including a requeue that succeeds and a requeue that dead-letters again. The operations document explains when to requeue and when not to.",
      "surface": "control plane",
      "dependsOn": [
        "p3-tables"
      ]
    },
    {
      "key": "p3-ac-webhook",
      "title": "Acceptance: a duplicate webhook produces one mission",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "acceptance-criterion",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "Five identical correctly signed inbound requests arriving concurrently start exactly one mission.",
      "acceptance": "The test posts five concurrent signed requests and asserts exactly one mission directory exists and exactly one inbox row is in the consumed state. The test is watched to fail against the pre-change ingress path.",
      "surface": "control plane",
      "dependsOn": [
        "p3-ingress-swap"
      ]
    },
    {
      "key": "p3-ac-retries",
      "title": "Acceptance: connector retries produce exactly one reply",
      "milestone": "MS-0004",
      "labels": [
        "task",
        "P3",
        "acceptance-criterion",
        "area:connector",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p3",
      "requirement": "A connector endpoint that rejects twice with a retry hint and then accepts receives exactly one message. A crash after the post, before the row is marked delivered, also results in exactly one message.",
      "acceptance": "The fake endpoint answers 429 with a retry hint twice then 200, and the test asserts one delivery. The crash variant sets the crash point after the post, restarts, and asserts the marker query prevents the second send. Both are watched to fail without the outbox.",
      "surface": "control plane",
      "dependsOn": [
        "p3-outbox-github"
      ]
    },
    {
      "key": "p4-mission-bridge",
      "title": "Mission to workflow translation",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "A mission translates into a workflow: the node list becomes a sequential chain, preserving today's ordering exactly. Job kinds are mission-node for the existing control-plane node kinds and sandbox-job for delivery steps under the v2 specification. Parallel execution is possible under the new model and is deliberately not turned on here, so that a behaviour difference in this milestone cannot be attributed to a scheduling change.",
      "acceptance": "A translation test asserts the produced dependency graph is a chain in the mission's node order. Local development still runs the v1 private runner path end to end.",
      "surface": "control plane",
      "dependsOn": [
        "p1-controller"
      ]
    },
    {
      "key": "p4-embedded-runner",
      "title": "Embedded control-plane runner in the chat server",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "The chat server runs an in-process runner with a mission-control label that claims mission-node jobs and executes the existing mission node bodies unchanged. The bodies are not rewritten in this step; only what invokes them changes. The chat server is loaded by path with module-scope monkeypatching, so no patched module-level function is renamed — wrap instead.",
      "acceptance": "A grep-based test asserts the module-level names the suite patches still exist. The full suite is green with the rollout flag on legacy, and green again with it on broker.",
      "surface": "control plane",
      "dependsOn": [
        "p4-mission-bridge"
      ]
    },
    {
      "key": "p4-approvals",
      "title": "Approval gates become a first-class waiting job state",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "area:orchestration",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "An approval gate is a waiting state on a job, and approving or resuming a mission signals that job through the shared command module. Separation of duties is retained at the point of signalling. The approval evidence is written before any successor is dispatched, in the same transaction, so that a crash between the two cannot produce work whose authorization was never recorded.",
      "acceptance": "A test asserts a successor is not dispatched until the approval evidence is committed, using a crash point between the two. Separation-of-duties tests pass unchanged. The thread-exits-and-is-relaunched path is gone.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "p4-embedded-runner"
      ]
    },
    {
      "key": "p4-resume",
      "title": "Missions resume after restart instead of being marked paused",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "Startup supervision is replaced: the sweeper reclaims dead mission-node leases and missions resume automatically. The field that recorded orphaned-at-startup is removed along with the tests that assert it, because a field that reports a failure to resume is not needed once the work resumes. Enumerate the affected tests by grepping for that field, the orchestrator launcher and the supervision entrypoint before writing any code.",
      "acceptance": "A restart test asserts a running mission continues rather than pausing. The removed field appears nowhere in the tree. The per-mission daemon threads are deleted, not bypassed.",
      "surface": "control plane",
      "dependsOn": [
        "p4-approvals"
      ]
    },
    {
      "key": "p4-evidence-archive",
      "title": "Evidence table and a restorable archive export",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "area:orchestration",
        "area:schema",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "Evidence is mirrored into a table carrying its digest, chain hashes and signature, so that an archive export captures workflow, approvals and evidence in one restorable artifact. Mission documents and the integrity chain stay authoritative and are referenced by digest — the table is an index, never a replacement.",
      "acceptance": "An export and import round-trip preserves the hash chain and verifies. A test asserts the table is not read as authority: corrupting a row is detected against the authoritative document rather than trusted.",
      "surface": "schemas (authority-owned)",
      "dependsOn": [
        "p4-resume"
      ]
    },
    {
      "key": "p4-flag-removal",
      "title": "Flip the orchestrator flag and delete the legacy path",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "The rollout flag defaults to legacy while tests migrate, flips to broker mid-phase, and the legacy orchestrator code and the flag itself are deleted at the end of the phase. A rollout flag left in place is a permanent fork with two code paths and one set of tests.",
      "acceptance": "The flag and the legacy orchestrator are absent from the tree. The full suite is green. The pull request states what was deleted and how a rollback would be performed if the flip proves wrong.",
      "surface": "control plane",
      "dependsOn": [
        "p4-evidence-archive"
      ]
    },
    {
      "key": "p4-ac-backup",
      "title": "Acceptance: backup and restore preserve integrity mid-approval",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "acceptance-criterion",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "An archive taken while a mission is waiting on an approval restores to a state where the hash chain verifies, the approval can still be completed, and no connector delivery is duplicated by the restore.",
      "acceptance": "The test round-trips an archive taken mid-approval, verifies the chain, completes the approval after restore, and asserts the connector delivery count is unchanged.",
      "surface": "control plane",
      "dependsOn": [
        "p4-evidence-archive",
        "p3-outbox-github"
      ]
    },
    {
      "key": "p4-ac-coordinator-mission",
      "title": "Acceptance: coordinator dies mid-mission, the mission resumes with no duplicate side effect",
      "milestone": "MS-0005",
      "labels": [
        "task",
        "P4",
        "acceptance-criterion",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p4",
      "requirement": "The P1 coordinator-death criterion holds for missions, not only for broker workflows. Missions were the path that previously stranded work on restart, so passing it at the broker level does not settle it.",
      "acceptance": "The mission variant of the coordinator-kill test passes: SIGKILL at a crash point, restart, mission completes, side-effect counter reads exactly 1, exactly one recovery ledger entry.",
      "surface": "control plane",
      "dependsOn": [
        "p4-resume"
      ]
    },
    {
      "key": "p5-inventory",
      "title": "Inventory every surface carrying the prior product name",
      "milestone": "MS-0006",
      "labels": [
        "task",
        "P5",
        "area:naming",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p5",
      "requirement": "A written inventory of every surface carrying the prior product name: executable names, environment variable prefixes, schema identifier namespaces, configuration keys, documentation and prose. Each entry says whether it is externally visible, because an externally visible name cannot simply be changed.",
      "acceptance": "The inventory is committed and counts match a fresh search of the tracked tree. Nothing is renamed in this issue.",
      "surface": "docs"
    },
    {
      "key": "p5-decision",
      "title": "Record the naming migration decision and the shim policy",
      "milestone": "MS-0006",
      "labels": [
        "task",
        "P5",
        "area:naming",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p5",
      "requirement": "A decision record states the target names, which compatibility shims are provided, how long each survives, and what removing one requires. A published schema identifier that has already been consumed is not renamed on a whim; the record says which ones those are.",
      "acceptance": "The decision record is committed and linked from the milestone record. Every subsequent rename issue cites it.",
      "surface": "docs",
      "dependsOn": [
        "p5-inventory"
      ]
    },
    {
      "key": "p5-binaries",
      "title": "Rename the executables with compatibility names",
      "milestone": "MS-0006",
      "labels": [
        "task",
        "P5",
        "area:naming",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p5",
      "requirement": "Executables are renamed to the Steel-Mission names, each keeping a working compatibility name for one release. The suite drives these as subprocesses by name, so this is a test-visible change in roughly two hundred places at once: land the shim first, migrate the call sites second, remove the shim third, with the suite green between each.",
      "acceptance": "The suite is green after each of the three steps. The compatibility names work and emit a deprecation notice. No behaviour changed: the pull request contains renames and shims only.",
      "surface": "worker",
      "dependsOn": [
        "p5-decision"
      ]
    },
    {
      "key": "p5-env",
      "title": "Migrate the environment variable prefix with dual reads",
      "milestone": "MS-0006",
      "labels": [
        "task",
        "P5",
        "area:naming",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p5",
      "requirement": "Environment variables move to the Steel-Mission prefix, reading both prefixes for one release with the new one taking precedence and the old one emitting a deprecation notice. An operator whose deployment sets the old names does not have a broken deployment on upgrade day.",
      "acceptance": "A test asserts both prefixes are read, that the new one wins where both are set, and that the old one warns. The suite is green.",
      "surface": "control plane",
      "dependsOn": [
        "p5-decision"
      ]
    },
    {
      "key": "p5-schema-ids",
      "title": "Migrate the schema identifier namespace",
      "milestone": "MS-0006",
      "labels": [
        "task",
        "P5",
        "area:naming",
        "security-review",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p5",
      "requirement": "Schema identifiers move to the Steel-Mission namespace, following the decision record on which already-published identifiers may move and which must not. An identifier is a contract with whoever validates against it; changing one silently breaks them.",
      "acceptance": "Every schema validates its fixtures after the change. The registry is consistent. Any identifier deliberately left on the old namespace is listed with the reason.",
      "surface": "schemas (authority-owned)",
      "dependsOn": [
        "p5-decision"
      ]
    },
    {
      "key": "p5-docs-sweep",
      "title": "Documentation and prose sweep",
      "milestone": "MS-0006",
      "labels": [
        "task",
        "P5",
        "area:naming",
        "area:docs",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p5",
      "requirement": "The README, architecture, operations, security, install, quickstart and protocol documents name one product. The only remaining references to the prior name are the migration provenance record and the compatibility shim table, both of which name it deliberately.",
      "acceptance": "A search of the tracked tree returns only those two deliberate references. A reader arriving at the README learns what Steel-Mission is without encountering a second product name.",
      "surface": "docs",
      "dependsOn": [
        "p5-binaries",
        "p5-env",
        "p5-schema-ids"
      ]
    },
    {
      "key": "u0-schemas",
      "title": "Add schemas for the configuration files and register them",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "The user registry, organisation registry, capability registry and auth policy each have a schema in schemas/canonical/ registered in the schema registry. Schemas land before the code that enforces them, per the repository's schema-first discipline. The runtime-profile and model-role schemas already exist and are reused rather than duplicated.",
      "acceptance": "Each new schema validates the shipped configuration file it describes, and rejects a fixture with the defect it is meant to catch. Registry entries are appended rather than the file re-sorted, so the diff shows the additions. No behaviour change yet: nothing validates on write until a later task.",
      "surface": "schemas (authority-owned)"
    },
    {
      "key": "u0-loader-both-shapes",
      "title": "Let the capability registry loader read both file shapes",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "The loader accepts the shipped shape and the normalized shape with a stated precedence, so the first save is no longer a one-way door. Today the writer always emits the normalized shape and the loader prefers it, after which the shipped arrays are never read again and later hand-edits are silently ignored. This lands before anything can call the writer: building the assignment interface first would arm a destructive flip that is currently unreachable from a browser.",
      "acceptance": "A test writes the shipped-shape registry, saves through the endpoint, reloads, and asserts the existing assignment still resolves. Watched to fail against the current loader. The shipped configuration file is unchanged by the round trip in any way that loses information.",
      "surface": "control plane",
      "dependsOn": [
        "u0-schemas"
      ]
    },
    {
      "key": "u0-fail-closed-parse",
      "title": "Fail closed when the capability registry cannot be parsed",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "A parse error is recorded and refused rather than silently answered with a different schema shape that grants the coordinator capability to two synthetic identifiers. A configuration file that cannot be read is an operational fault, not a cue to invent a working default.",
      "acceptance": "A test corrupts the registry and asserts the failure is surfaced and recorded, and that no synthetic assignment appears. Watched to fail against the current fallback.",
      "surface": "control plane",
      "dependsOn": [
        "u0-loader-both-shapes"
      ]
    },
    {
      "key": "u0-user-crosscheck",
      "title": "Refuse capability assignments for users that do not exist",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "An assignment naming a user identifier absent from the user registry is refused with the offending list, rather than persisted and re-emitted as though it were real. There is no cross-check today.",
      "acceptance": "A test posts an assignment for an unknown identifier and asserts a refusal naming it, and that the registry is unchanged. Watched to fail.",
      "surface": "control plane",
      "dependsOn": [
        "u0-loader-both-shapes"
      ]
    },
    {
      "key": "u0-refuse-user-registry",
      "title": "Stop the user registry coercing what it cannot represent",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "An empty user list is refused rather than silently repopulating four default accounts with no identity subjects. A suspended status is refused or honoured rather than coerced to active — today attempting to suspend a user re-enables them. An unknown capability key is reported rather than dropped.",
      "acceptance": "Three tests, each watched to fail: an empty post is refused and the registry is unchanged; a suspended status does not come back active; an unknown capability key is named in the response rather than silently removed. Existing tests that relied on a coercion are changed deliberately in this pull request, with the reason stated.",
      "surface": "control plane",
      "dependsOn": [
        "u0-schemas"
      ]
    },
    {
      "key": "u0-refuse-organization",
      "title": "Stop an organisation with no capabilities being granted all of them",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "An organisation saved with zero domain capabilities keeps zero. Today an empty list is falsy and the code substitutes every valid capability, so removing all capabilities from an organisation grants it all thirteen. The same pattern applies to knowledge domains.",
      "acceptance": "A test saves an organisation with an empty capability list and asserts it has none, then does the same for knowledge domains. Watched to fail against the current substitution.",
      "surface": "control plane",
      "dependsOn": [
        "u0-schemas"
      ]
    },
    {
      "key": "u0-refuse-assignments-wipe",
      "title": "Stop an empty assignments post wiping the register",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "A malformed or truncated assignments body is refused rather than emptying every capability assignment and answering 200.",
      "acceptance": "A test posts an empty body and asserts a refusal with the register intact. Watched to fail.",
      "surface": "control plane",
      "dependsOn": [
        "u0-loader-both-shapes"
      ]
    },
    {
      "key": "u0-refuse-worktree-mode",
      "title": "Refuse an unrecognised worktree mode instead of mutating the checkout",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "An unrecognised worktree mode is refused. Today it is silently forced to in-place, which mutates the source checkout, behind a 202 that reports acceptance. This is the most dangerous coercion in the set because the caller asked for isolation and got the opposite.",
      "acceptance": "A test requests an unrecognised mode and asserts a refusal naming the accepted values, and that no checkout was touched. Watched to fail.",
      "surface": "control plane",
      "dependsOn": [
        "u0-schemas"
      ]
    },
    {
      "key": "u0-validate-on-write",
      "title": "Validate every configuration write against its schema",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "Every configuration writer validates against its registered schema before it replaces the file, using the existing standard-library validator rather than a new dependency. This is what makes the individual refusals permanent rather than a list of patches that the next writer forgets.",
      "acceptance": "A test asserts each writer refuses a payload that violates its schema, and that the file on disk is unchanged after a refusal. The mutation ledger records the refusal.",
      "surface": "control plane",
      "dependsOn": [
        "u0-schemas",
        "u0-refuse-user-registry",
        "u0-refuse-organization"
      ]
    },
    {
      "key": "u0-capability-authz",
      "title": "Enforce capability authorization in every identity mode",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity",
        "security-review"
      ],
      "epic": "epic-u0",
      "requirement": "Capability enforcement on mission bindings no longer depends on the identity mode. Today it is skipped unless the mode is provider-required, and the shipped mode is not, so any actor can bind any capability to any mission. Any development allowance becomes an explicit key in the auth policy rather than an implied consequence of a mode branch.",
      "acceptance": "A test asserts a publisher cannot bind a capability they are not assigned, in the shipped identity mode. Every existing binding test is checked before the change and any that depended on the gap is updated deliberately. If the shipped demo needs an allowance, it is a written key with a stated reason, not a silent branch.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "u0-user-crosscheck"
      ]
    },
    {
      "key": "u0-identity-precedence",
      "title": "A declared role never outranks a resolved registered user",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity",
        "security-review"
      ],
      "epic": "epic-u0",
      "requirement": "Where a request resolves to an active registered user, that user's access level decides what they may do; a role asserted in a request header cannot exceed it. The loopback development fallback is kept, because it is a deliberate and tested boundary rather than an oversight.",
      "acceptance": "A test asserts a header-declared owner role does not grant owner access when the resolved user is a publisher. The existing tests pinning loopback-accepted and remote-refused behaviour still pass unchanged.",
      "surface": "authentication, authorization, or session handling",
      "dependsOn": [
        "u0-capability-authz"
      ]
    },
    {
      "key": "u0-runtime-profile-unknown-id",
      "title": "Stop fabricating a runtime profile for an unknown identifier",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "An unknown profile identifier is refused. The offline fallback for a known profile is legitimate and stays; fabricating a profile that carries the requested identifier and recording it in a mission as valid is not, because the mission then carries a profile that never existed.",
      "acceptance": "A test starts a mission against an unknown profile identifier and asserts a refusal. A second asserts the offline fallback still works for a known identifier. Watched to fail. The runtime-profile tests pass unchanged.",
      "surface": "control plane"
    },
    {
      "key": "u0-status-codes",
      "title": "One mapping from failure class to status code",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "A permission failure answers 403 and a validation failure answers 400, through one wrapper the admin handlers share. Today the same failure class answers 400 in one handler group and 403 in another purely because of which exception each catches.",
      "acceptance": "A test asserts the same refusal produces the same status from every admin endpoint. The wrapper is used by all of them.",
      "surface": "control plane"
    },
    {
      "key": "u0-assignments-tests",
      "title": "Give the capability registry endpoint tests",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "The only writer of the capability registry has authorization and round-trip tests. It currently has neither, and no interface calls it, so it has never been exercised outside a unit-level save.",
      "acceptance": "Tests cover: a non-owner is refused; an owner round-trips an assignment; a refusal leaves the file unchanged. It is now safe to build an interface on.",
      "surface": "control plane",
      "dependsOn": [
        "u0-loader-both-shapes",
        "u0-user-crosscheck"
      ]
    },
    {
      "key": "u0-capability-ownership",
      "title": "Resolve the two capability assignment mechanisms into one",
      "milestone": "MS-0007",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U0",
        "area:config-integrity"
      ],
      "epic": "epic-u0",
      "requirement": "There is one place a capability assignment lives. Today there are two: the publisher and user arrays in the capability registry, which are read only when no active user of that role carries any assigned capabilities and then grant the capability to the role collectively; and the assigned capabilities on each user, which is what actually has effect. Pick one, delete the other, and migrate the shipped configuration. Once one mechanism exists, assign the four capabilities that currently have no owner — Counterweight, Creative, Platform and Synthesis — or record deliberately that they are unowned.",
      "acceptance": "A test asserts a capability resolves through exactly one path. The shipped configuration is migrated in the same pull request. The four previously unowned capabilities are either assigned or listed as unowned with a reason. Assigning them before this task would write into the dead mechanism and appear to work.",
      "surface": "control plane",
      "dependsOn": [
        "u0-assignments-tests"
      ]
    },
    {
      "key": "u1-vocabulary-endpoint",
      "title": "Serve the vocabulary from the server instead of string literals",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "One endpoint returns the term table and the capability list with display names, so labels come from the registry the server already publishes rather than from literals scattered through the page. This is what kills the hardcoded capability array and the duplicated key at the source rather than fixing each label by hand.",
      "acceptance": "The endpoint returns every capability with its key and display name. A test asserts it matches the knowledge registry. No label in the page is a literal capability key after the dependent tasks land.",
      "surface": "control plane"
    },
    {
      "key": "u1-contract-test",
      "title": "A behavioural contract test, green against the page that ships today",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "A contract test asserts behaviour rather than implementation: the route answers, the capability list is registry-derived, a label never prints its key twice, the capability definition is present for a non-manager access level, the work-mode control carries a description. It is green against the current page before anything is rebuilt. This test is what makes the 231 implementation-pinning assertions retirable at the flip; without it they are deleted with nothing replacing them.",
      "acceptance": "The test file exists and is green. Each assertion was watched to fail against the page as it ships today, and the failure is quoted in the pull request.",
      "surface": "chat / UI",
      "dependsOn": [
        "u1-vocabulary-endpoint"
      ]
    },
    {
      "key": "u1-registry-derived-labels",
      "title": "Derive capability labels from the registry, and print each key once",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "Capability checkboxes read as one legible label instead of the key printed twice followed by the name. The hardcoded capability array is removed and both renderers use one registry-derived path. The duplication exists because one field is overwritten from the registry to equal the other, so joining both prints it twice.",
      "acceptance": "A test asserts no label repeats its key and that no hardcoded capability array remains. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u1-contract-test"
      ]
    },
    {
      "key": "u1-coordinator-model-label",
      "title": "Say what the picker actually selects",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "The control that chooses which model executes the Delivery Coordinator is labelled as that, with a one-line description, instead of being labelled Profile beside a system that also has thirteen Domain Capabilities and three other meanings of the word Profile. This is the specific confusion that started the project.",
      "acceptance": "A test asserts the label and description are present and that the word Profile does not appear adjacent to a capability list. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u1-contract-test"
      ]
    },
    {
      "key": "u1-capability-definition-visible",
      "title": "Tell publishers and users what a Domain Capability is",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "The definition of a Domain Capability is visible to every access level. Today it is inside an owner and admin branch, so the publishers and users who are assigned capabilities are the only people never told what they have been assigned.",
      "acceptance": "A test asserts the definition appears for a non-manager access level. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u1-contract-test"
      ]
    },
    {
      "key": "u1-work-mode-explained",
      "title": "Make the work-mode switch state what it changes",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "The work-mode control carries a visible group label and a one-line description of each mode. Today it is an unlabelled pair of buttons that changes a placeholder and four starter prompts, defaults to the non-obvious setting, and is explained nowhere in the product.",
      "acceptance": "A test asserts the group label and both descriptions are present and reachable to assistive technology. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u1-contract-test"
      ]
    },
    {
      "key": "u1-lexicon-test",
      "title": "A test that refuses the banned collocations",
      "milestone": "MS-0008",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U1",
        "area:ui"
      ],
      "epic": "epic-u1",
      "requirement": "A lexicon test refuses the collocations the vocabulary forbids: Profile adjacent to a capability list, Role labelling a capability control, a bare capability key in prose, and the two different Profile labels appearing as user-visible text at once. Prose does not bind anyone; a failing test does.",
      "acceptance": "The test is green and each rule was watched to fail against a deliberate violation.",
      "surface": "chat / UI",
      "dependsOn": [
        "u1-registry-derived-labels",
        "u1-coordinator-model-label"
      ]
    },
    {
      "key": "u2-bundler-constraints",
      "title": "Prove the bundler output satisfies the two existing tests, first",
      "milestone": "MS-0009",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "epic": "epic-u2",
      "requirement": "Before a single component is written, real build output is checked against the two existing tests that constrain it: the suite matches a bare script element, so a module-typed script matches nothing; and it runs a syntax check on the extracted script as an ordinary script file, where a top-level import is a syntax error. The bundle must therefore be emitted in immediately-invoked format.",
      "acceptance": "A minimal build produces output that passes the existing script-parse test unchanged. If it cannot, that test is changed deliberately in this pull request with the reason written down — never quietly at the flip.",
      "surface": "CI / tooling"
    },
    {
      "key": "u2-toolchain",
      "title": "Pin the three-package toolchain with a lockfile",
      "milestone": "MS-0009",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "epic": "epic-u2",
      "requirement": "The build depends on exactly three direct packages at exact versions, with a committed lockfile carrying integrity hashes, installed only from that lockfile. The dependency budget is stated as a number so that keeping it small is checkable rather than aspirational.",
      "acceptance": "The manifest pins exact versions with no ranges. A clean install from the lockfile succeeds. The direct and transitive counts are recorded in the pull request.",
      "surface": "CI / tooling",
      "dependsOn": [
        "u2-bundler-constraints"
      ]
    },
    {
      "key": "u2-build-script",
      "title": "Build one self-contained page and commit it",
      "milestone": "MS-0009",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "epic": "epic-u2",
      "requirement": "The build emits a single self-contained page with no runtime network fetch, and the output is committed. This is what keeps the server reading one file path with no static-asset route, keeps the container unchanged, and keeps the test suite free of a toolchain. Minification stays off through the migration so the committed diff is reviewable.",
      "acceptance": "Make targets build and check the page. The output is one file and fetches nothing at runtime. The server is unchanged.",
      "surface": "CI / tooling",
      "dependsOn": [
        "u2-toolchain"
      ]
    },
    {
      "key": "u2-ci-drift-gate",
      "title": "Prove the committed page is a rebuild, not a hand edit",
      "milestone": "MS-0009",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "epic": "epic-u2",
      "requirement": "Continuous integration installs from the lockfile, audits, type-checks, runs the front-end unit tests, rebuilds and fails on any difference against the committed page. A file that a clean checkout reproduces byte for byte cannot be a hand-edited bundle. The interpreter matrix stays free of the toolchain.",
      "acceptance": "The job is a required status check and passes on a clean checkout. A deliberately edited committed page fails it, then is reverted. The determinism scope is stated rather than assumed.",
      "surface": "CI / tooling",
      "dependsOn": [
        "u2-build-script"
      ]
    },
    {
      "key": "u2-flag-route",
      "title": "Serve the new application behind a flag at a non-default route",
      "milestone": "MS-0009",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "epic": "epic-u2",
      "requirement": "The function the router calls keeps its name and gains a selector; the legacy and application renderers are new names, because the harness patches module-level names and a rename stops being patched without failing. The default stays the page that ships today. The head-request handler takes its length from the file on disk and must be corrected in the same pull request, or it reports a wrong length the moment the served page is not that file.",
      "acceptance": "Both pages are reachable and both pass the contract test. The default is unchanged and all existing page assertions stay green. A test asserts the head-request length matches the served body.",
      "surface": "control plane",
      "dependsOn": [
        "u2-build-script",
        "u1-contract-test"
      ]
    },
    {
      "key": "u2-supply-chain-controls",
      "title": "Put the new dependencies under the same guard as the rest",
      "milestone": "MS-0009",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U2",
        "area:ui"
      ],
      "epic": "epic-u2",
      "requirement": "The package files are owned in the code-owners file alongside the other supply-chain surfaces, and the dependency updater covers the new ecosystem with the same treatment as the container images, because a build that runs with repository credentials is a real change.",
      "acceptance": "The code-owners entry exists and the updater opens a pull request for the new ecosystem. Both verified rather than assumed.",
      "surface": "CI / tooling",
      "dependsOn": [
        "u2-toolchain"
      ]
    },
    {
      "key": "u3-settings-ia",
      "title": "Give settings an information architecture",
      "milestone": "MS-0010",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "epic": "epic-u3",
      "requirement": "Configuration is organised so a person can find the thing they came for. Today eight sections share one dialog whose element identifiers are all named after profiles while its label says Settings, and the vocabulary of the identifiers and the labels has already diverged in the source.",
      "acceptance": "The contract test asserts each section is reachable and named by the agreed vocabulary. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u2-flag-route"
      ]
    },
    {
      "key": "u3-capability-workspace",
      "title": "Make the capability workspace render for the people it is for",
      "milestone": "MS-0010",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "epic": "epic-u3",
      "requirement": "A person can see which capabilities they hold. The panel written for this renders for nobody today: it clears itself for owners and admins, and publishers and users can never make its navigation section active, so the one surface designed to answer the question reaches no one.",
      "acceptance": "A test asserts a publisher and a user each see their assigned capabilities, and that the empty state names how to get one. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u3-settings-ia"
      ]
    },
    {
      "key": "u3-assignment-editor",
      "title": "Give the capability registry endpoint an interface",
      "milestone": "MS-0010",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "epic": "epic-u3",
      "requirement": "An owner can assign a capability from the console. The only writer of the capability registry has no interface today and is reachable only by a hand-crafted request. This is deliberately last among the capability work: it is safe only once the loader accepts both shapes and the endpoint has tests.",
      "acceptance": "A test drives an assignment through the interface and asserts it round-trips. A non-owner cannot reach it.",
      "surface": "chat / UI",
      "dependsOn": [
        "u3-capability-workspace",
        "u0-assignments-tests"
      ]
    },
    {
      "key": "u3-parity-panels",
      "title": "Bring the remaining panels to parity",
      "milestone": "MS-0010",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "epic": "epic-u3",
      "requirement": "Every panel the previous console offered is present and works, one panel per pull request with the contract test extended for each. Parity plus the named defects is the boundary; anything beyond it is a new milestone rather than a larger this one.",
      "acceptance": "The contract test covers each panel. Every call the new application makes already existed before this milestone.",
      "surface": "chat / UI",
      "dependsOn": [
        "u3-settings-ia"
      ]
    },
    {
      "key": "u3-unowned-capabilities-visible",
      "title": "Show an unowned capability as unowned, not blank",
      "milestone": "MS-0010",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "epic": "epic-u3",
      "requirement": "A capability with no owner is displayed as unowned. Four capabilities have no owner today and the console shows nothing, so the gap is invisible in exactly the place someone would notice it.",
      "acceptance": "A test asserts an unowned capability renders as unowned with a way to assign one. Watched to fail.",
      "surface": "chat / UI",
      "dependsOn": [
        "u3-capability-workspace"
      ]
    },
    {
      "key": "u3-flip-default",
      "title": "Flip the default and retire the implementation pins",
      "milestone": "MS-0010",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U3",
        "area:ui"
      ],
      "epic": "epic-u3",
      "requirement": "The rebuilt application becomes the default page, and the 231 assertions that pin implementation strings in the previous page are deleted in the same pull request. Deleting them is permissible only because the contract test has been green for two milestones and covers the behaviour they stood for.",
      "acceptance": "The full suite is green with the new default. Every deleted pin is covered by a named contract assertion, and the mapping is stated in the pull request. Reverting the default is a one-line environment change, demonstrated.",
      "surface": "chat / UI",
      "dependsOn": [
        "u3-parity-panels",
        "u3-assignment-editor",
        "u3-unowned-capabilities-visible"
      ]
    },
    {
      "key": "u4-remove-legacy",
      "title": "Remove the superseded page and its flag",
      "milestone": "MS-0011",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U4",
        "area:ui"
      ],
      "epic": "epic-u4",
      "requirement": "The previous page and the selector flag are deleted rather than parked. A rollout flag left in place is a permanent fork with two surfaces and one set of tests — the same rule this repository applies to the orchestrator flag in the other project.",
      "acceptance": "The page and the flag are absent from the tree. The suite is green. The pull request states what was deleted and how a rollback would be performed.",
      "surface": "chat / UI",
      "dependsOn": [
        "u3-flip-default"
      ]
    },
    {
      "key": "u4-standing-rule-and-resume",
      "title": "Record the standing dependency rule and resume PRJ-0001",
      "milestone": "MS-0011",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U4",
        "area:docs"
      ],
      "epic": "epic-u4",
      "requirement": "The workplan states the front-end dependency rule and the number the audit enforces, so the next person does not relitigate it. PRJ-0001 returns to active with its target dates re-set from the day it resumes, and its estimate re-defended at the boundary with the delta printed rather than absorbed.",
      "acceptance": "The workplan carries the rule. The project record reads active with dates that are true on the day they are written.",
      "surface": "docs",
      "dependsOn": [
        "u4-remove-legacy"
      ]
    },
    {
      "key": "u5-image-excludes-toolchain",
      "title": "Keep the front-end toolchain out of the published image",
      "milestone": "MS-0012",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U5",
        "area:ci",
        "security-review"
      ],
      "epic": "epic-u5",
      "requirement": "The published image carries no front-end toolchain under /workspace. It currently ships 43 MB of /workspace/node_modules plus the UI source directory, because the image copies the tree wholesale and the exclusion list never gained them. This contradicts the reversibility condition recorded on PRJ-0000 that the image gains no node runtime dependency for the product, and it puts a JavaScript dependency tree inside a Python runtime image on a product whose case rests on a supply chain one person can review. The toolchain is a build-time tool for people changing the front end; the committed page is what the product serves. Scope matters here: /usr/local/lib/node_modules is 519 MB of Claude and Codex provider tooling that the Dockerfile copies deliberately, and node itself is required by those tools. Leave them alone -- an acceptance criterion that says no node_modules at all cannot pass.",
      "acceptance": "A rebuilt image contains no /workspace/node_modules and no /workspace front-end source directory, shown by inspecting the image rather than by reading the ignore file, with the provider tooling under /usr/local/lib still present and the CLIs still resolving. The size before and after is recorded in the pull request. A test fails when the exclusion list would let either back in, so this cannot silently return: today's guard checks only that .env, plan and tooling are named, and a fourth entry that is missing is invisible to it. The Dockerfile itself is unchanged.",
      "surface": "subprocess or container execution"
    },
    {
      "key": "u5-capability-ownership-complete",
      "title": "Give every domain capability an owner or a recorded reason",
      "milestone": "MS-0012",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U5",
        "area:ci"
      ],
      "epic": "epic-u5",
      "requirement": "Each of the thirteen domain capabilities either resolves to an owner or is recorded as deliberately unowned with a reason. Four that had no owner were assigned during MS-0007; DC07 Intelligence and DC10 Ecosystem were not, because the instruction that named the four was itself incomplete. They are unowned today and were unowned before, so this is not a regression, but an unowned capability that nobody decided about is indistinguishable from one that was overlooked -- which is how the first four came to be overlooked.",
      "acceptance": "A test asserts every capability key in the registry resolves to an owner or appears in a recorded list of deliberate exceptions carrying a reason. The two currently unowned capabilities are resolved either way in this pull request, and the choice is stated rather than implied by the file.",
      "surface": "control plane"
    },
    {
      "key": "u5-runtime-writes-leave-the-tree-clean",
      "title": "Stop the running product modifying version-controlled configuration",
      "milestone": "MS-0012",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U5",
        "area:ci"
      ],
      "epic": "epic-u5",
      "requirement": "Running the product does not modify files that are under version control. Starting the server and using it rewrites config/users.json in place -- normalised, reordered and restamped with a new producer and timestamp -- so development and testing leave the working tree dirty and a real change becomes hard to see in the noise. The normalisation itself is not the problem; writing it back over product configuration is.",
      "acceptance": "Starting the server against a clean checkout, exercising the paths that trigger a normalising write, and running git status shows no modification. Whatever behaviour depended on the write still works, with a test covering it. If the write is genuinely required, it goes somewhere outside the product tree and that location is documented.",
      "surface": "control plane"
    },
    {
      "key": "u5-loopback-session-resolves-to-a-real-identity",
      "title": "Give a fresh loopback session a real identity instead of a capability-less ghost",
      "milestone": "MS-0012",
      "labels": [
        "task",
        "prj:PRJ-0000",
        "stage:U5",
        "area:identity",
        "security-review"
      ],
      "epic": "epic-u5",
      "requirement": "A loopback console session with nothing in local storage resolves to a registered user, not to a synthetic actor that matches no one. The console's first call is GET /api/auth/whoami with no X-Present-Role or X-Present-Actor header (steel-mission-ui/src/app.tsx:33-38,63). The server's loopback-development fallback (steel-mission-chat/server.py:2787-2794) reads the role from the URL path; \"auth\" is not in {owner, admin, publisher, user}, so it defaults to role \"user\", actor id \"user\". No user with id \"user\" exists in config/users.json, so registered_user() finds nothing and the actor is hard-coded to capabilities: []. Verified against the running server: curl http://127.0.0.1:8765/api/auth/whoami with no headers returns actorId \"user\", role \"user\", capabilities []. accessLevel comes from that same role, so the console shows the capability empty state, and Settings -> Users -- gated to owner or admin -- refuses it too. There is no path from a fresh browser to the owner account (morgan-vale) that already exists in the registry, even though hitting /api/owner/users directly resolves the same kind of phantom actor to role \"owner\" and succeeds -- the console's own identity check just never asks under that path. This is the exact failure a fresh operator hits: the start screen, the message telling them to ask an owner or admin, and no way to become one.",
      "acceptance": "A test calls /api/auth/whoami as a loopback request with no stored identity and asserts the resolved actor can reach at least Settings -> Users, or is otherwise handed a real registered identity -- not a role string that matches nobody in the user registry. The fix stays inside the development-local loopback trust boundary already documented for this mode; oidc-required behaviour is untouched, and no login screen or identity picker is added. A clean checkout, started fresh with an empty browser profile, reaches a console that is not a dead end.",
      "surface": "authentication, authorization, or session handling"
    },
    {
      "key": "c0-owner-capability-truth",
      "title": "Show owner and admin their real Domain Capabilities",
      "milestone": "MS-0013",
      "labels": [
        "task",
        "C0",
        "area:identity",
        "prj:PRJ-0001",
        "security-review"
      ],
      "epic": "epic-c0",
      "requirement": "The capability workspace derives owner and admin capabilities the way the server already does, instead of reading the raw per-user assignment field. Today capabilityWorkspaceView (steel-mission-ui/src/capabilities.ts) filters actor.capabilities, which is empty for owner and admin by design, while corporate_workspace (steel-mission-chat/server.py, visibleCapabilities) grants those roles all thirteen unconditionally. The result is the registered owner landing on 'No Domain Capabilities are assigned yet' with an instruction to ask an owner - a screen that lies about authority to the one person who has all of it.",
      "acceptance": "A fresh session as the registered owner renders all thirteen capabilities, demonstrated against the running server and asserted by a test watched to fail first. The derivation has one source of truth: the page consumes the workspace grant, and no console code path reads raw assignedCapabilities for display again. Publisher and user views are unchanged, covered by the existing tests.",
      "surface": "authentication, authorization, or session handling"
    },
    {
      "key": "c0-stale-identity-fallback",
      "title": "Resolve a stale declared identity to the registered owner",
      "milestone": "MS-0013",
      "labels": [
        "task",
        "C0",
        "area:identity",
        "prj:PRJ-0001",
        "security-review"
      ],
      "epic": "epic-c0",
      "requirement": "A loopback request whose declared X-Present-Actor matches no registered user resolves the way a request with no declared identity does, instead of fabricating a capability-less actor. The fix for #169 covered only the no-headers path (authenticate_http_request, steel-mission-chat/server.py); the declared-but-unresolvable branch still hard-codes capabilities: [], and the console persists identity headers in localStorage, so one stale value re-creates the dead end the fix closed.",
      "acceptance": "A loopback request with X-Present-Actor naming an unregistered user resolves to the registered owner, asserted by a test watched to fail first. The audit trail records that a declared identity was discarded and why. oidc-required behaviour is untouched, shown by the existing identity-boundary tests.",
      "surface": "authentication, authorization, or session handling"
    },
    {
      "key": "c0-truthful-sidebar-and-status-strip",
      "title": "Bind the sidebar and a provider status strip to real state",
      "milestone": "MS-0013",
      "labels": [
        "task",
        "C0",
        "area:ui",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c0",
      "requirement": "The console reports server, authority and provider state from the endpoints that already know it. 'Server: Not connected' and 'Authority: Advisory' are hardcoded JSX (steel-mission-ui/src/app.tsx) even when /api/health returns live coordinator readiness including a real local-model probe, and /api/control-plane/readiness returns authority state. A status strip shows, per provider - claude, codex, glimmer - connected, online, idle or working, with token usage where the provider reports it.",
      "acceptance": "Stopping and starting the local model observably changes the strip, demonstrated against the running server. No status string in the built page is a constant: a test greps the committed page for the retired literals and fails if they return. Idle versus working reflects live job state, asserted by a test that starts a job and reads the strip's source endpoint.",
      "surface": "none of the above — console rendering only"
    },
    {
      "key": "c1-chat-composer",
      "title": "Give the console a chat surface on the existing pipeline",
      "milestone": "MS-0014",
      "labels": [
        "task",
        "C1",
        "area:ui",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c1",
      "requirement": "The landing screen carries a chat composer wired to the pipeline the server already ships: POST /api/chat, job polling, follow-ups and decisions (steel-mission-chat/server.py) have been orphaned since the legacy console was deleted in PR #162 - the machinery answers, no page asks. The composer streams a conversation with the coordinator under the active runtime profile.",
      "acceptance": "A Person types a question on the landing screen and receives the coordinator's answer, demonstrated against the running server with the local model. The orphaned endpoints have a consumer again, covered by page-level tests. The plain form at /plain is unchanged.",
      "surface": "none of the above — console rendering only"
    },
    {
      "key": "c1-work-modes",
      "title": "Make the two work modes real, with a model picker",
      "milestone": "MS-0014",
      "labels": [
        "task",
        "C1",
        "area:ui",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c1",
      "requirement": "The existing work-mode toggle stops being decoration. Normal mode is a plain chat carrying a model picker over the registered coordinator providers; Domain-Capabilities mode is the capability workspace with the delivery setup. Codex becomes the normal-mode default when MS-0015 lands; until then the picker offers what the registry can actually route.",
      "acceptance": "Switching modes changes the surface, and the picker's selection changes which provider answers, both demonstrated against the running server and asserted by tests. The picker never offers a provider the registry cannot route, proven by a test against the resolution endpoint.",
      "surface": "none of the above — console rendering only",
      "dependsOn": [
        "c1-chat-composer"
      ]
    },
    {
      "key": "c1-session-status-feed",
      "title": "Define the session status feed as the draft of job protocol v2",
      "milestone": "MS-0014",
      "labels": [
        "task",
        "C1",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c1",
      "requirement": "One schema describes the state of an agent session - claimed issue, stage, worker, machine account, started, last event, budget spent, outcome - written as append-only JSONL. This is the durable artifact of the bench under D7: MS-0002 persists it in the store and MS-0003's job protocol carries it, so it is designed as a contract, not a log format.",
      "acceptance": "The schema exists under schemas/ with valid and invalid fixtures, and a validator test covers both. The bench writes it and the progress panel reads it with no intermediate translation. A schema-authority review confirms the shape before P1 consumes it.",
      "surface": "control plane"
    },
    {
      "key": "c1-mission-pipeline-bench",
      "title": "Run granted missions through the four-stage pipeline bench",
      "milestone": "MS-0014",
      "labels": [
        "task",
        "C1",
        "area:orchestration",
        "prj:PRJ-0001",
        "security-review"
      ],
      "epic": "epic-c1",
      "requirement": "A tooling-level bench executes a granted mission unattended through the D8 pipeline: plan by Claude at Opus 5 or above; develop and commit by the local model in an isolated worktree under its machine account; a bounded review loop by Codex; final review, approval and merge by Claude at Opus 5 or above, with auto-merge armed so the approval is what lands it. The bench claims the issue by assignment, feeds the agent exactly the issue's requirement and acceptance text, enforces the workplan's gates mechanically - no push while the suite is red, failing-test evidence in the pull request body - refuses security-review-labelled issues, honours per-session time and turn budgets, and escalates through the existing user-decision functionality when the plan proves unclean. It lives in tooling/, never product code, and is disposable by declaration under D7.",
      "acceptance": "A granted rehearsal mission runs the full pipeline on a real issue to a merged pull request with no human action after the grant, and the evidence pack records every stage. A deliberately unclean plan escalates and waits, then resumes on the Person's decision, asserted by a test. The bench cannot merge: branch protection is the wall, and the merge fires only on the acceptance account's approval. Killing a session leaves the main checkout untouched, shown by git status.",
      "surface": "subprocess or container execution",
      "dependsOn": [
        "c1-session-status-feed"
      ]
    },
    {
      "key": "c1-progress-panel",
      "title": "Show pipeline progress in the console",
      "milestone": "MS-0014",
      "labels": [
        "task",
        "C1",
        "area:ui",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c1",
      "requirement": "A read-only panel renders the session status feed: which worker holds which issue, the stage each mission is in, idle versus working, budget spent, and what is waiting on the Person. It binds to the feed format, not to the bench, so it survives the bench's retirement unchanged.",
      "acceptance": "Live bench sessions render in the panel, demonstrated with two concurrent sessions. The panel is read-only: it exposes no control that mutates a session. A feed entry the schema rejects renders as an error state rather than being silently skipped.",
      "surface": "none of the above — console rendering only",
      "dependsOn": [
        "c1-session-status-feed"
      ]
    },
    {
      "key": "c1-coordinator-pending-decisions",
      "title": "Let the coordinator answer what needs the Person",
      "milestone": "MS-0014",
      "labels": [
        "task",
        "C1",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c1",
      "requirement": "The coordinator's snapshot includes pending-decision state - escalated missions, waiting approvals, blocked sessions - so asking the coordinator what needs me returns a truthful, current answer drawn from records rather than the model's memory of the conversation.",
      "acceptance": "With one escalated mission outstanding, asking the coordinator what needs the Person names it, demonstrated against the running server. With nothing outstanding the answer says so plainly. The snapshot source is the decision state itself, asserted by a test that injects a pending decision and reads the snapshot bundle.",
      "surface": "control plane",
      "dependsOn": [
        "c1-session-status-feed"
      ]
    },
    {
      "key": "c2-codex-coordinator",
      "title": "Make Codex a first-class coordinator provider",
      "milestone": "MS-0015",
      "labels": [
        "task",
        "C2",
        "area:orchestration",
        "prj:PRJ-0001",
        "security-review"
      ],
      "epic": "epic-c2",
      "requirement": "Codex joins claude and glimmer as a coordinator provider and becomes the normal-mode chat default. The provider set is pinned in four independent layers today: the request schemas (coordination-report-request-v1, runtime-profile-resolution-v1, model-role-registry-v1), the worker dispatch (bin/present-worker), the server allowlists (steel-mission-chat/server.py), and the page. codex_adapter gains a coordinator_report with the same schema-constrained envelope the other two adapters return, and a codex runtime profile becomes selectable.",
      "acceptance": "A chat is answered by Codex with provider and model visible in the status strip, demonstrated against the running server. Every widened enum carries an updated fixture, and the routing contract tests cover the third provider including its unavailable state, which fails closed to the registry's fallback rather than substituting silently. The full suite is green.",
      "surface": "subprocess or container execution"
    },
    {
      "key": "c2-registry-model-plumbing",
      "title": "Make the registry's model choice govern the actual call",
      "milestone": "MS-0015",
      "labels": [
        "task",
        "C2",
        "area:config-integrity",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c2",
      "requirement": "The model id and effort resolved from config/model-role-registry.json flow into the adapter invocations. Today cmd_coordinator_report (bin/present-worker) calls the adapters without a model argument, so hardcoded defaults run regardless of the registry - the selection is display and audit metadata only, which means the Settings page can silently have no effect. With the plumbing in place, the planner and acceptance roles are set to Opus 5 or above and Codex to its maximum available model and reasoning setting.",
      "acceptance": "Changing the registry's model id changes the invoked model, shown from the invocation record rather than the display, asserted by a test watched to fail first. The maximum settings are recorded in the registry, and a chat demonstrates each role running its declared model. A registry model the provider does not recognise is refused at resolution time with a named error, never silently mapped to a default.",
      "surface": "control plane"
    },
    {
      "key": "c2-glimmer-rewarm",
      "title": "Re-warm the local model instead of refusing after idle expiry",
      "milestone": "MS-0015",
      "labels": [
        "task",
        "C2",
        "area:orchestration",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c2",
      "requirement": "A chat that arrives after the local model's keep-alive has expired triggers a reload instead of a refusal. Today glimmer readiness requires the model loaded (adapters/glimmer_adapter.py), Ollama unloads after thirty idle minutes, and the next request returns glimmer_not_ready until someone re-runs the manual start - a dead end on a machine where the model is installed and the server is up. The deliberate refusal to cold-start the server process itself stays: re-warm covers reloading a model into a running server, not booting one.",
      "acceptance": "A chat issued more than thirty idle minutes after the last one succeeds without manual intervention, with the re-warm visible in the status strip while it happens. The memory floor check still gates an actual server start, covered by the existing tests. A re-warm that fails reports the failure truthfully rather than hanging the chat.",
      "surface": "control plane"
    },
    {
      "key": "c2-token-usage-capture",
      "title": "Capture and surface token usage per provider",
      "milestone": "MS-0015",
      "labels": [
        "task",
        "C2",
        "area:ui",
        "prj:PRJ-0001"
      ],
      "epic": "epic-c2",
      "requirement": "Each coordinator invocation records the token usage its provider reports - Ollama returns evaluation counts natively, the CLI stream events carry usage - and the status strip surfaces it per provider. Where a provider reports nothing, the strip says unknown rather than zero.",
      "acceptance": "After a chat, the strip shows the tokens that invocation reported, matching the invocation record. A provider that reports no usage shows unknown, asserted by a test. Usage lands in the evidence trail with the invocation it belongs to.",
      "surface": "none of the above — console rendering only"
    },
    {
      "key": "p1-status-feed-persistence",
      "title": "Persist the session status feed in the durable store",
      "milestone": "MS-0002",
      "labels": [
        "task",
        "P1",
        "area:store",
        "prj:PRJ-0001"
      ],
      "epic": "epic-p1",
      "requirement": "The store persists agent-session status in the schema the C1 feed established, so pipeline progress survives a restart the same way jobs do and the progress panel reads one source whether the bench or the broker produced it.",
      "acceptance": "A session recorded before a daemon SIGKILL reads back identically after recovery, added to the coordinator-kill test. The feed schema is the C1 schema unchanged; any divergence is a schema change with a fixture, not a drift. The panel renders store-backed sessions with no code change, demonstrated.",
      "surface": "control plane",
      "dependsOn": [
        "c1-session-status-feed"
      ]
    },
    {
      "key": "p2-agent-executor",
      "title": "Run coding agents as runner jobs, and retire the bench",
      "milestone": "MS-0003",
      "labels": [
        "task",
        "P2",
        "area:runner",
        "prj:PRJ-0001",
        "security-review"
      ],
      "epic": "epic-p2",
      "requirement": "The runner gains an agent-executor job class: a coding agent runs in an isolated worktree with a commit step, its machine-account git identity, credentials scoped to exactly the job's declared set, a network policy admitting exactly the model endpoints the job declares, and timeout ceilings sized for agent runs rather than the current shell-command limits. The current delivery path cannot host this by design: the scoped environment strips agent credentials, Docker mode defaults to no network, commands are capped at 500 characters and 180 seconds, and no node commits (steel-mission-chat/server.py, bin/present-private-runner). The D8 pipeline runs through broker and runner, and the C1 bench retires.",
      "acceptance": "A granted mission runs plan, develop and commit, review loop, and final review to a merged pull request through the broker and a runner agent, with the bench retired and the retirement recorded. The executor's environment is asserted by test: credentials present are exactly the declared set, and a sentinel secret never appears in arguments, logs, store or artifacts. The existing shell-command delivery path is unchanged, covered by its current tests.",
      "surface": "subprocess or container execution",
      "dependsOn": [
        "c1-mission-pipeline-bench",
        "p1-status-feed-persistence"
      ]
    }
  ]
}

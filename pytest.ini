[pytest]
markers =
    postgres: requires the optional PostgreSQL backend and a configured test database
    docker: requires a working Docker daemon and locally built test images
    slow: exercises timing, retries, or whole-process recovery and may take longer

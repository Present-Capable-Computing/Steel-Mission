{
  "name": "steel-mission-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned build-only toolchain for the committed Steel Mission UI",
  "scripts": {
    "ui:build": "node tooling/build-ui.mjs",
    "ui:check": "node tooling/build-ui.mjs --check",
    "ui:test": "node tooling/test-ui.mjs",
    "ui:typecheck": "tsc --project steel-mission-ui/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "preact": "10.29.8",
    "typescript": "7.0.2"
  }
}
